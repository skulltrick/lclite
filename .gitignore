# installer scratch
selection.json
*.log

# local dev / scratch — this folder is copied INTO a Lost City checkout and
# must never carry upstream content into the overlay repo
t/
test/
scratch/

node_modules/
__pycache__/
.DS_Store
Thumbs.db

# launcher builds (release artifacts, not repo content)
LCLite.exe
LCLite.exe~
launcher/LCLite.exe
launcher/LCLite-*
launcher/dist/
*~
