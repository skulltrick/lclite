{
 "file": "webclient/src/client/Client.ts",
 "mod": "true-tile",
 "rev": "289",
 "generated_from": {
  "repo": "webclient",
  "head": "a52b321a9f42530f3dd2f1a02cd335c955791a12"
 },
 "hunks": [
  {
   "find": [
    "        }",
    "",
    "        const cycle = Pix3D.cycle;",
    "        Model.mouseCheck = true;"
   ],
   "replace": [
    "        }",
    "",
    "        // lclite:true-tile",
    "        /// custom (lclite \"true-tile\" mod): hand World the tile the SERVER has the player",
    "        /// on (routeX/routeZ[0] — the head of the move queue the local move codes push",
    "        /// into) and the tile the MODEL is on, for the \"only while desynced\" option. World",
    "        /// draws the decal itself, as ground geometry, on that tile's own turn in the",
    "        /// scene fill order, so everything the engine draws after it — the player's model",
    "        /// above all — covers it (RuneLite's true tile reads the same way: the square is",
    "        /// on the floor under the character). Settings are read there too, at the mod's",
    "        /// own hook (rule 5), and the fill's alpha rides Pix3D.trans into both the",
    "        /// software raster and the gpu mod's per-triangle capture.",
    "        if (this.localPlayer) {",
    "            World.trueTileArm(this.localPlayer.routeX[0], this.localPlayer.routeZ[0], this.localPlayer.x >> 7, this.localPlayer.z >> 7, this.minusedlevel);",
    "        }",
    "",
    "        const cycle = Pix3D.cycle;",
    "        Model.mouseCheck = true;"
   ],
   "note": "src/client/Client.ts @ old line 4237",
   "occ": 1,
   "pos": [
    4237,
    0,
    4235,
    4238
   ]
  }
 ]
}