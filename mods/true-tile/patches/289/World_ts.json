{
 "file": "webclient/src/dash3d/World.ts",
 "mod": "true-tile",
 "rev": "289",
 "generated_from": {
  "repo": "webclient",
  "head": "a52b321a9f42530f3dd2f1a02cd335c955791a12"
 },
 "hunks": [
  {
   "find": [
    "import type ModelSource from '#/dash3d/ModelSource.js';",
    "import type PointNormal from '#/dash3d/PointNormal.js';",
    "",
    "const PRETAB = Uint8Array.of(19, 55, 38, 155, 255, 110, 137, 205, 76);"
   ],
   "replace": [
    "import type ModelSource from '#/dash3d/ModelSource.js';",
    "import type PointNormal from '#/dash3d/PointNormal.js';",
    "// lclite:true-tile",
    "import { destTileRead, destTileSettings, tileDecal, trueTileRead, trueTileSettings } from '#/dash3d/TrueTile.js'; // custom (lclite \"true-tile\" mod): the settings parse + the tile-quad geometry its three markers share",
    "",
    "const PRETAB = Uint8Array.of(19, 55, 38, 155, 255, 110, 137, 205, 76);"
   ],
   "note": "src/dash3d/World.ts @ old line 22",
   "occ": 1,
   "pos": [
    22,
    0,
    20,
    23
   ]
  },
  {
   "find": [
    "    static groundX: number = -1;",
    "    static groundZ: number = -1;",
    "",
    "    private static visBacking: boolean[][][][] = new TypedArray4d(8, 32, 51, 51, false);"
   ],
   "replace": [
    "    static groundX: number = -1;",
    "    static groundZ: number = -1;",
    "    // lclite:true-tile",
    "    /// custom (lclite \"true-tile\" mod, hovered-tile section): the mouse-hover ground pick.",
    "    /// World.click/groundX above answer \"which tile did the player click\" (one-shot,",
    "    /// consumed by the walk); these answer \"which tile is the mouse over right now\",",
    "    /// armed every frame by Client.hoverTileArm() and resolved by the SAME triangle test",
    "    /// the click pick uses, in renderQuickGround/renderGround. hoverLevel is the level",
    "    /// that ground was DRAWN at — the square's own originalLevel, which World.pushDown()",
    "    /// moves down a level for LinkBelow tiles — or -1 on the legacy Ground path, which has",
    "    /// no level in scope (the client then falls back to the engine's own getAvH for the",
    "    /// player's level).",
    "    static hoverArmed: boolean = false;",
    "    static hoverMouseX: number = 0;",
    "    static hoverMouseY: number = 0;",
    "    static hoverX: number = -1;",
    "    static hoverZ: number = -1;",
    "    static hoverLevel: number = -1;",
    "    /// custom (lclite \"true-tile\" mod, destination section): the tile the player is",
    "    /// walking to, armed every frame by Client.gameDrawMain() from the engine's OWN",
    "    /// destination (Client.minimapFlagX/Z — tryMove() sets it to the path's destination",
    "    /// tile, the engine zeroes it when the player's model reaches it, and 0 is its \"no",
    "    /// destination\" sentinel). World.destTileDecal draws it as ground geometry on that",
    "    /// tile's own turn in fill(), like the true tile, and gives way the moment the true",
    "    /// tile is on it: the marker is gone the tick the SERVER has you there, not a tick",
    "    /// later when the model catches up.",
    "    static destTileArmed: boolean = false;",
    "    static destTileX: number = -1;",
    "    static destTileZ: number = -1;",
    "    static destTileLevel: number = 0;",
    "",
    "    private static visBacking: boolean[][][][] = new TypedArray4d(8, 32, 51, 51, false);"
   ],
   "note": "src/dash3d/World.ts @ old line 114",
   "occ": 1,
   "pos": [
    114,
    0,
    112,
    115
   ]
  },
  {
   "find": [
    "    }",
    "",
    "    getWall(level: number, x: number, z: number): Wall | null {",
    "        const tile: Square | null = this.squares[level][x][z];"
   ],
   "replace": [
    "    }",
    "",
    "    // lclite:true-tile",
    "    /// custom (lclite \"true-tile\" mod): the true tile's World-side state and draw. The",
    "    /// client arms the tile once per frame (Client.gameDrawMain, before renderAll) and",
    "    /// fill() draws it on that tile's OWN turn in the back-to-front ground pass — after",
    "    /// the tile's ground, before its walls and sprites — so the decal sits on the floor",
    "    /// and everything drawn later covers it: the player's model above all, which is the",
    "    /// whole point (RuneLite's true tile reads as a square UNDER the character, not over",
    "    /// it). Drawn as Pix3D triangles rather than buffer pixels for two reasons: buffer",
    "    /// pixels written during the scene pass are HUD to the gpu mod and ride up OVER the",
    "    /// model, and Pix3D.trans is what carries the fill's alpha into both the software",
    "    /// raster and the gpu capture. Geometry + settings live in the mod's files/ payload",
    "    /// (dash3d/TrueTile.ts); this mod reads its OWN keys at this hook (rule 5).",
    "    private static trueTileArmed: boolean = false;",
    "    private static trueTileX: number = -1;",
    "    private static trueTileZ: number = -1;",
    "    private static trueTileModelX: number = -1;",
    "    private static trueTileModelZ: number = -1;",
    "    private static trueTileLevel: number = 0;",
    "",
    "    /// Arm the decal for this frame's render. Called by Client.gameDrawMain() before",
    "    /// renderAll, so the projection below uses the camera the scene is drawn with.",
    "    static trueTileArm(tileX: number, tileZ: number, modelX: number, modelZ: number, level: number): void {",
    "        this.trueTileArmed = true;",
    "        this.trueTileX = tileX;",
    "        this.trueTileZ = tileZ;",
    "        this.trueTileModelX = modelX;",
    "        this.trueTileModelZ = modelZ;",
    "        this.trueTileLevel = level;",
    "    }",
    "",
    "    /// Arm the destination decal for this frame's render, from the client's own",
    "    /// destination tile. `tileX` 0 is the engine's own \"no destination\" sentinel, and it",
    "    /// disarms the decal instead of arming tile 0.",
    "    static destTileArm(tileX: number, tileZ: number, level: number): void {",
    "        this.destTileArmed = tileX > 0;",
    "        this.destTileX = tileX;",
    "        this.destTileZ = tileZ;",
    "        this.destTileLevel = level;",
    "    }",
    "",
    "    /// The ground height at a point on a level, EXACTLY as the ground rasterizer reads it:",
    "    /// the bilinear sample of groundh[level] with NO LinkBelow promotion. Client.getAvH()",
    "    /// is the same sample PLUS that promotion — right for \"how high is the player",
    "    /// standing\" (every engine caller passes the player's level), wrong for \"how high was",
    "    /// this ground DRAWN\": World.pushDown() moves a LinkBelow tile's squares down a level,",
    "    /// so the level the rasterizer drew with is already the promoted one, and promoting it",
    "    /// again lifts the sample a whole level (240 units) into the air.",
    "    groundHeight(level: number, sceneX: number, sceneZ: number): number {",
    "        const tileX: number = sceneX >> 7;",
    "        const tileZ: number = sceneZ >> 7;",
    "",
    "        if (!this.groundh || level < 0 || level >= this.maxTileLevel || tileX < 0 || tileZ < 0 || tileX >= this.maxTileX - 1 || tileZ >= this.maxTileZ - 1) {",
    "            return 0;",
    "        }",
    "",
    "        const tileLocalX: number = sceneX & 0x7f;",
    "        const tileLocalZ: number = sceneZ & 0x7f;",
    "        const y00: number = (this.groundh[level][tileX][tileZ] * (128 - tileLocalX) + this.groundh[level][tileX + 1][tileZ] * tileLocalX) >> 7;",
    "        const y11: number = (this.groundh[level][tileX][tileZ + 1] * (128 - tileLocalX) + this.groundh[level][tileX + 1][tileZ + 1] * tileLocalX) >> 7;",
    "        return (y00 * (128 - tileLocalZ) + y11 * tileLocalZ) >> 7;",
    "    }",
    "",
    "    /// Project a tile's four ground corners into the 512x334 buffer at the ground height",
    "    /// of `heightLevel`, with the SAME camera math the client's own overlays use",
    "    /// (Client.getOverlayPos, and renderQuickGround above), so the quad shears over slopes",
    "    /// and stairs exactly like the floor it covers. False = a corner is behind the camera,",
    "    /// so this frame has nothing to draw (every overlay bails the same way).",
    "    private tileQuad(tileX: number, tileZ: number, heightLevel: number, px: number[], py: number[]): boolean {",
    "        const x0: number = tileX << 7;",
    "        const z0: number = tileZ << 7;",
    "",
    "        for (let i: number = 0; i < 4; i++) {",
    "            const x: number = i === 1 || i === 2 ? x0 + 128 : x0;",
    "            const z: number = i === 2 || i === 3 ? z0 + 128 : z0;",
    "",
    "            let dx: number = x - World.cx;",
    "            let dy: number = this.groundHeight(heightLevel, x, z) - World.cy;",
    "            let dz: number = z - World.cz;",
    "",
    "            let tmp: number = (dz * World.cameraSinY + dx * World.cameraCosY) >> 16;",
    "            dz = (dz * World.cameraCosY - dx * World.cameraSinY) >> 16;",
    "            dx = tmp;",
    "",
    "            tmp = (dy * World.cameraCosX - dz * World.cameraSinX) >> 16;",
    "            dz = (dy * World.cameraSinX + dz * World.cameraCosX) >> 16;",
    "            dy = tmp;",
    "",
    "            if (dz < 50) {",
    "                return false;   // a corner is behind the camera: skip the frame, like every overlay",
    "            }",
    "",
    "            px[i] = Pix3D.originX + (((dx << 9) / dz) | 0);",
    "            py[i] = Pix3D.originY + (((dy << 9) / dz) | 0);",
    "        }",
    "",
    "        return true;",
    "    }",
    "",
    "    /// Bind this mod's own Pix3D surface and emit one decal: `trans` is the mix's",
    "    /// DESTINATION weight (0 = opaque) and both the software raster and the gpu mod's",
    "    /// per-triangle capture honour it. hclip is the caller's job — World's own ground",
    "    /// draws set it the same way — so a partially off-screen quad clamps to the buffer",
    "    /// instead of wrapping a row.",
    "    private tileDecalDraw(px: number[], py: number[], rgb: number, thick: number, fillA: number): void {",
    "        const savedTrans: number = Pix3D.trans;",
    "        const savedHclip: boolean = Pix3D.hclip;",
    "        Pix3D.hclip = true;",
    "        tileDecal((xA: number, xB: number, xC: number, yA: number, yB: number, yC: number, colour: number, trans: number): void => {",
    "            Pix3D.trans = trans;",
    "            Pix3D.flatTriangle(xA, xB, xC, yA, yB, yC, colour);",
    "        }, px, py, rgb, thick, fillA);",
    "        Pix3D.trans = savedTrans;",
    "        Pix3D.hclip = savedHclip;",
    "    }",
    "",
    "    /// The armed tile's decal, drawn from fill() when that tile is reached. One shot per",
    "    /// frame: the first (and only) matching tile disarms it, so a tile the fill queue",
    "    /// hands back twice can never double-blend its translucent fill.",
    "    ///",
    "    /// `level` is the scene loop's level — what the client armed, and the gate for the",
    "    /// match. `heightLevel` is the level this tile's GROUND was drawn at:",
    "    /// Square.originalLevel, the same value fill() hands the rasterizer and groundOccluded.",
    "    /// They differ exactly where World.pushDown() has moved a LinkBelow tile's squares",
    "    /// down a level (a bridge deck, a hillside): the deck's square sits at loop level 0",
    "    /// while its ground is level 1's, and reading groundh[0] there put the true tile in",
    "    /// the water under the bridge. Reading the drawn level puts it on the deck — which is",
    "    /// where the player's own model is, because Client.getAvH() promotes the player's level",
    "    /// for exactly the same tiles.",
    "    private trueTileDecal(tileX: number, tileZ: number, level: number, heightLevel: number): void {",
    "        if (!World.trueTileArmed || tileX !== World.trueTileX || tileZ !== World.trueTileZ || level !== World.trueTileLevel) {",
    "            return;",
    "        }",
    "",
    "        World.trueTileArmed = false;",
    "",
    "        const settings = trueTileSettings(trueTileRead);   // this mod's OWN keys, at its own hook",
    "        if (!settings.enabled) {",
    "            return;",
    "        }",
    "",
    "        if (settings.onlyDesync && World.trueTileModelX === tileX && World.trueTileModelZ === tileZ) {",
    "            return;   // RuneLite's \"hidden\" behaviour: nothing worth pointing at while in sync",
    "        }",
    "",
    "        const px: number[] = [0, 0, 0, 0];",
    "        const py: number[] = [0, 0, 0, 0];",
    "        if (!this.tileQuad(tileX, tileZ, heightLevel, px, py)) {",
    "            return;",
    "        }",
    "",
    "        this.tileDecalDraw(px, py, settings.rgb, settings.thick, settings.fillA);",
    "    }",
    "",
    "    /// The armed destination tile's decal (the destination section of this mod), drawn",
    "    /// from fill() exactly like the true tile's — same turn in the ground pass, same",
    "    /// drawn-level rule for the height. Two differences: its settings are its OWN keys",
    "    /// (rule 5), and it gives way to the true tile — the moment the tile the server has",
    "    /// the player on IS the destination there is nothing left to point at, so the marker",
    "    /// disappears on the arrival tick rather than waiting for the engine to zero its flag",
    "    /// a tick later.",
    "    private destTileDecal(tileX: number, tileZ: number, level: number, heightLevel: number): void {",
    "        if (!World.destTileArmed || tileX !== World.destTileX || tileZ !== World.destTileZ || level !== World.destTileLevel) {",
    "            return;",
    "        }",
    "",
    "        World.destTileArmed = false;",
    "",
    "        if (tileX === World.trueTileX && tileZ === World.trueTileZ) {",
    "            return;   // the true tile is already here: the walk is over",
    "        }",
    "",
    "        const settings = destTileSettings(destTileRead);   // this mod's OWN keys, at its own hook",
    "        if (!settings.enabled) {",
    "            return;",
    "        }",
    "",
    "        const px: number[] = [0, 0, 0, 0];",
    "        const py: number[] = [0, 0, 0, 0];",
    "        if (!this.tileQuad(tileX, tileZ, heightLevel, px, py)) {",
    "            return;",
    "        }",
    "",
    "        this.tileDecalDraw(px, py, settings.rgb, settings.thick, settings.fillA);",
    "    }",
    "",
    "    getWall(level: number, x: number, z: number): Wall | null {",
    "        const tile: Square | null = this.squares[level][x][z];"
   ],
   "note": "src/dash3d/World.ts @ old line 505",
   "occ": 1,
   "pos": [
    505,
    0,
    503,
    506
   ]
  },
  {
   "find": [
    "                }",
    "",
    "                let direction: number = 0;",
    "                let frontWallTypes: number = 0;"
   ],
   "replace": [
    "                }",
    "",
    "                // lclite:true-tile",
    "                if (tileDrawn) {",
    "                    // custom (lclite \"true-tile\" mod) — on this tile's own turn: over its ground,",
    "                    // under its walls and sprites. `level` is the scene loop's level (what the",
    "                    // client armed); originalLevel is the level this square's GROUND was drawn at,",
    "                    // which World.pushDown() moves down a level for LinkBelow tiles (a bridge deck,",
    "                    // a hillside) — so the decals land on the surface under the player instead of",
    "                    // the ground beneath it.",
    "                    this.trueTileDecal(tileX, tileZ, level, originalLevel);",
    "                    this.destTileDecal(tileX, tileZ, level, originalLevel); // custom (lclite \"true-tile\" mod, destination section)",
    "                }",
    "",
    "                let direction: number = 0;",
    "                let frontWallTypes: number = 0;"
   ],
   "note": "src/dash3d/World.ts @ old line 1491",
   "occ": 1,
   "pos": [
    1491,
    0,
    1489,
    1492
   ]
  },
  {
   "find": [
    "            if (World.click && this.insideTriangle(World.clickX, World.clickY, pz1, py3, px1, py1, px3, pz0)) {",
    "                World.groundX = tileX;",
    "                World.groundZ = tileZ;",
    "            }",
    "",
    "            if (ground.texture !== -1) {",
    "                if (!World.lowMem) {"
   ],
   "replace": [
    "            if (World.click && this.insideTriangle(World.clickX, World.clickY, pz1, py3, px1, py1, px3, pz0)) {",
    "                World.groundX = tileX;",
    "                World.groundZ = tileZ;",
    "            }",
    "            // lclite:true-tile",
    "            if (World.hoverArmed && this.insideTriangle(World.hoverMouseX, World.hoverMouseY, pz1, py3, px1, py1, px3, pz0)) {",
    "                World.hoverX = tileX;",
    "                World.hoverZ = tileZ;",
    "                World.hoverLevel = level;",
    "            }",
    "",
    "            if (ground.texture !== -1) {",
    "                if (!World.lowMem) {"
   ],
   "note": "src/dash3d/World.ts @ old line 2003",
   "occ": 1,
   "pos": [
    2003,
    0,
    1999,
    2005
   ]
  },
  {
   "find": [
    "            if (World.click && this.insideTriangle(World.clickX, World.clickY, py0, px1, py3, px0, pz0, px3)) {",
    "                World.groundX = tileX;",
    "                World.groundZ = tileZ;",
    "            }",
    "",
    "            if (ground.texture !== -1) {",
    "                if (!World.lowMem) {"
   ],
   "replace": [
    "            if (World.click && this.insideTriangle(World.clickX, World.clickY, py0, px1, py3, px0, pz0, px3)) {",
    "                World.groundX = tileX;",
    "                World.groundZ = tileZ;",
    "            }",
    "            // lclite:true-tile",
    "            if (World.hoverArmed && this.insideTriangle(World.hoverMouseX, World.hoverMouseY, py0, px1, py3, px0, pz0, px3)) {",
    "                World.hoverX = tileX;",
    "                World.hoverZ = tileZ;",
    "                World.hoverLevel = level;",
    "            }",
    "",
    "            if (ground.texture !== -1) {",
    "                if (!World.lowMem) {"
   ],
   "note": "src/dash3d/World.ts @ old line 2055",
   "occ": 1,
   "pos": [
    2055,
    0,
    2051,
    2057
   ]
  },
  {
   "find": [
    "                    World.groundZ = tileZ;",
    "                }",
    "",
    "                if (ground.faceTexture && ground.faceTexture[v] !== -1) {"
   ],
   "replace": [
    "                    World.groundZ = tileZ;",
    "                }",
    "                // lclite:true-tile",
    "                if (World.hoverArmed && this.insideTriangle(World.hoverMouseX, World.hoverMouseY, y0, y1, y2, x0, x1, x2)) {",
    "                    World.hoverX = tileX;",
    "                    World.hoverZ = tileZ;",
    "                    World.hoverLevel = -1;",
    "                }",
    "",
    "                if (ground.faceTexture && ground.faceTexture[v] !== -1) {"
   ],
   "note": "src/dash3d/World.ts @ old line 2141",
   "occ": 1,
   "pos": [
    2141,
    0,
    2139,
    2142
   ]
  }
 ]
}