{
 "file": "webclient/src/client/Client.ts",
 "mod": "hotkeys",
 "generated_from": {
  "repo": "webclient",
  "head": "a52b321a9f42530f3dd2f1a02cd335c955791a12"
 },
 "hunks": [
  {
   "find": [
    "import Skill from '#/client/Skill.js';",
    "import TitleFlames from '#/client/TitleFlames.js';",
    "",
    "import FloType from '#/config/FloType.js';"
   ],
   "replace": [
    "import Skill from '#/client/Skill.js';",
    "import TitleFlames from '#/client/TitleFlames.js';",
    "// lclite:hotkeys",
    "import {",
    "    HOTKEYS_CAMERA, HOTKEYS_CLEAR_CHAT, HOTKEYS_CLOSE, HOTKEYS_PASS, HOTKEYS_TAB,",
    "    HOTKEYS_KEYS, HOTKEYS_TABS, hotkeysDecide, hotkeysKeyName, hotkeysLocked, hotkeysReadSettings",
    "} from '#/client/Hotkeys.js'; // custom (lclite \"hotkeys\" mod): key tables + the pure claim decision",
    "",
    "import FloType from '#/config/FloType.js';"
   ],
   "note": "src/client/Client.ts @ old line 11",
   "occ": 1,
   "pos": [
    11,
    0,
    9,
    12
   ]
  },
  {
   "find": [
    "    }",
    "",
    "    // todo: order",
    "    private drawChat(): void {"
   ],
   "replace": [
    "    }",
    "",
    "    // lclite:hotkeys — keybinds: F-key sidebar tabs, Esc closes interfaces, WASD camera.",
    "    // Fields and methods sit in ONE isolated slot (the stat-orbs/true-tile pattern) so",
    "    // regen can never merge this with a neighbour's hunk. Settings are this mod's OWN",
    "    // localStorage keys (hard rule 5): read at key time, three keys per frame for the",
    "    // chatbox prompt. All decision logic is pure and lives in Hotkeys.ts.",
    "    /// custom: chat-lock state (true = the chatbox is open for typing) lclite:hotkeys",
    "    private hotkeysTyping: boolean = false;",
    "    /// custom: camera directions this mod is holding, by key name lclite:hotkeys",
    "    private hotkeysHeld: { [key: string]: number } = {};",
    "",
    "    /// custom: read this mod's own settings — Hotkeys.ts owns every default lclite:hotkeys",
    "    private hotkeysSettings() {",
    "        return hotkeysReadSettings((key: string, def: string) => localStorage.getItem(key) ?? def);",
    "    }",
    "",
    "    /// custom: an input dialogue / social box / report-abuse form owns the keyboard lclite:hotkeys",
    "    private hotkeysTextInput(): boolean {",
    "        if (this.dialogInputOpen || this.socialInputOpen) {",
    "            return true;",
    "        }",
    "        return this.mainModalId !== -1 && this.mainModalId === this.reportAbuseComId;",
    "    }",
    "",
    "    /// custom: is this main modal the character-design screen? (the player_kit family;",
    "    /// custom: Esc must not close it — it has no close button and the tutorial cannot",
    "    /// custom: continue without it. Detected by the CC_DESIGN_PREVIEW client code,",
    "    /// custom: which exactly one interface in the 289 table carries: 3559.) lclite:hotkeys",
    "    private hotkeysDesignScreen(rootId: number): boolean {",
    "        const walk = (id: number, depth: number): boolean => {",
    "            if (depth > 8) {",
    "                return false;",
    "            }",
    "            const com: IfType | null = IfType.list[id] ?? null;",
    "            if (!com || !com.children) {",
    "                return false;",
    "            }",
    "            for (let i: number = 0; i < com.children.length; i++) {",
    "                const child: IfType | null = IfType.list[com.children[i]] ?? null;",
    "                if (!child) {",
    "                    continue;",
    "                }",
    "                if (child.clientCode === ClientCode.CC_DESIGN_PREVIEW) {",
    "                    return true;",
    "                }",
    "                if (child.type === 0 && walk(com.children[i], depth + 1)) {",
    "                    return true;",
    "                }",
    "            }",
    "            return false;",
    "        };",
    "        return walk(rootId, 0);",
    "    }",
    "",
    "    /// custom: drop every camera key this mod holds (mod toggled off mid-hold) lclite:hotkeys",
    "    private hotkeysReleaseAll(): void {",
    "        for (const key in this.hotkeysHeld) {",
    "            this.keyHeld[this.hotkeysHeld[key]] = 0;",
    "        }",
    "        this.hotkeysHeld = {};",
    "    }",
    "",
    "    /// custom: switch the sidebar tab exactly the way clicking its icon does; a side",
    "    /// custom: interface (bank, shop) owns the sidebar, so it closes first — the key",
    "    /// custom: has to be visible, not hidden behind a modal that is still up lclite:hotkeys",
    "    private hotkeysSwitchTab(index: number): void {",
    "        if (index < 0 || index >= this.sideIcon.length || this.sideIcon[index] === -1) {",
    "            return; // the server has no interface in this slot (tutorial, empty slot)",
    "        }",
    "        if (this.sideModalId !== -1) {",
    "            this.closeModal();",
    "        }",
    "        this.activeIcon = index;",
    "        this.redrawSide = true;",
    "        this.redrawIcons = true;",
    "    }",
    "",
    "    /// custom: is the chatbox showing \"Press Enter to Chat...\" this frame? lclite:hotkeys",
    "    private hotkeysChatPrompt(): boolean {",
    "        return hotkeysLocked((key: string, def: string) => localStorage.getItem(key) ?? def, this.hotkeysTyping);",
    "    }",
    "",
    "    /// custom: GameShell hook — claim this mod's keys before the engine queues them lclite:hotkeys",
    "    protected override hotkeyKeyDown(e: KeyboardEvent): boolean {",
    "        const settings = this.hotkeysSettings();",
    "        if (!settings.on || !settings.wasd) {",
    "            this.hotkeysReleaseAll();",
    "        }",
    "",
    "        const key: string = hotkeysKeyName(e.key);",
    "        const result = hotkeysDecide({",
    "            key,",
    "            printable: e.key.length === 1,",
    "            modified: e.ctrlKey || e.altKey || e.metaKey,",
    "            settings,",
    "            typing: this.hotkeysTyping,",
    "            chatLen: this.chatInput.length,",
    "            modalOpen: this.mainModalId !== -1 || this.sideModalId !== -1 || this.chatModalId !== -1,",
    "            textInputOpen: this.hotkeysTextInput(),",
    "            inGame: this.ingame,",
    "            designScreen: this.mainModalId !== -1 && this.hotkeysDesignScreen(this.mainModalId)",
    "        });",
    "",
    "        this.hotkeysTyping = result.typing;",
    "",
    "        if (result.action === HOTKEYS_TAB) {",
    "            this.hotkeysSwitchTab(result.tab);",
    "        } else if (result.action === HOTKEYS_CLOSE) {",
    "            this.closeModal();",
    "        } else if (result.action === HOTKEYS_CLEAR_CHAT) {",
    "            this.chatInput = '';",
    "            this.redrawChat = true;",
    "        } else if (result.action === HOTKEYS_CAMERA) {",
    "            this.keyHeld[result.dir] = 1;",
    "            this.hotkeysHeld[key] = result.dir;",
    "        }",
    "",
    "        return result.action !== HOTKEYS_PASS;",
    "    }",
    "",
    "    /// custom: GameShell hook — the release side (a held WASD camera key clears here) lclite:hotkeys",
    "    protected override hotkeyKeyUp(e: KeyboardEvent): boolean {",
    "        const key: string = hotkeysKeyName(e.key);",
    "        const dir: number | undefined = this.hotkeysHeld[key];",
    "        if (dir !== undefined) {",
    "            this.keyHeld[dir] = 0;",
    "            delete this.hotkeysHeld[key];",
    "            return true;",
    "        }",
    "        return false;",
    "    }",
    "",
    "    // todo: order",
    "    private drawChat(): void {"
   ],
   "note": "src/client/Client.ts @ old line 11172",
   "occ": 1,
   "pos": [
    11172,
    0,
    11170,
    11173
   ]
  },
  {
   "find": [
    "",
    "            font?.drawString(username + ':', 4, 90, Colour.BLACK);",
    "            font?.drawString(this.chatInput + '*', font.stringWid(username + ': ') + 6, 90, Colour.BLUE);",
    "",
    "            Pix2D.hline(0, 77, 479, Colour.BLACK);"
   ],
   "replace": [
    "",
    "            font?.drawString(username + ':', 4, 90, Colour.BLACK);",
    "            // lclite:hotkeys — a locked chatbox (WASD camera + press-enter-to-chat) prompts for Enter",
    "            font?.drawString(this.hotkeysChatPrompt() ? 'Press Enter to Chat...' : this.chatInput + '*', font.stringWid(username + ': ') + 6, 90, Colour.BLUE);",
    "",
    "            Pix2D.hline(0, 77, 479, Colour.BLACK);"
   ],
   "note": "src/client/Client.ts @ old line 11311",
   "occ": 1,
   "pos": [
    11310,
    1,
    11308,
    11312
   ]
  }
 ]
}