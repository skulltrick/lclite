{
 "file": "engine/view/client.ejs",
 "mod": "control-panel",
 "generated_from": {
  "repo": "engine",
  "head": "0c7cf6555d0cf92d2348999b2fd27c6ceff5ffbf"
 },
 "hunks": [
  {
   "find": [
    "                case '2':",
    "                case '3':",
    "                    canvas.style.width = (765 * parseInt(size)) + 'px';",
    "                    canvas.style.height = (503 * parseInt(size)) + 'px';",
    "                    canvas.style.maxWidth = 'none';",
    "                    canvas.style.maxHeight = 'none';"
   ],
   "replace": [
    "                case '2':",
    "                case '3':",
    "                    // lclite:control-panel",
    "                    // custom: ANY decimal scale, not just 1x/2x/3x — clamped to",
    "                    // 0.25x..8x. '1'/'2'/'3' parse to themselves, so old saves and the",
    "                    // legacy dropdown keep behaving exactly as before.",
    "                    let lcmScaleFix = parseFloat(size);",
    "                    if (!isFinite(lcmScaleFix) || lcmScaleFix <= 0) { lcmScaleFix = 1; }",
    "                    if (lcmScaleFix < 0.25) { lcmScaleFix = 0.25; }",
    "                    else if (lcmScaleFix > 8) { lcmScaleFix = 8; }",
    "                    canvas.style.width = (765 * lcmScaleFix) + 'px';",
    "                    canvas.style.height = (503 * lcmScaleFix) + 'px';",
    "                    // lclite:control-panel",
    "                    canvas.style.maxWidth = 'none';",
    "                    canvas.style.maxHeight = 'none';"
   ],
   "note": "view/client.ejs @ old line 264",
   "occ": 1,
   "pos": [
    263,
    2,
    261,
    266
   ]
  },
  {
   "find": [
    "                    break;",
    "            }",
    "            document.getElementById('size').value = size;",
    "            localStorage.setItem('canvasSize', size);",
    "        }",
    ""
   ],
   "replace": [
    "                    break;",
    "            }",
    "            // lclite:control-panel",
    "            // custom: canvasSize stays THE key the page reads at boot ('auto' or a",
    "            // decimal). canvasScale remembers the last FIXED scale for the panel's",
    "            // scale slider / Fit toggle, and the legacy dropdown gets a \"custom\"",
    "            // option appended for odd values (1.35x) so it never loses its selection.",
    "            let lcmSize = String(size);",
    "            if (lcmSize !== 'auto') {",
    "                let lcmScaleSaved = parseFloat(lcmSize);",
    "                if (!isFinite(lcmScaleSaved) || lcmScaleSaved <= 0) { lcmScaleSaved = 1; }",
    "                else if (lcmScaleSaved < 0.25) { lcmScaleSaved = 0.25; }",
    "                else if (lcmScaleSaved > 8) { lcmScaleSaved = 8; }",
    "",
    "                lcmSize = String(lcmScaleSaved);",
    "                localStorage.setItem('canvasScale', lcmSize);",
    "            }",
    "",
    "            let lcmSizeSelect = document.getElementById('size');",
    "            if (lcmSizeSelect) {",
    "                let lcmSizeOption = null;",
    "                for (let i = 0; i < lcmSizeSelect.options.length; i++) {",
    "                    if (lcmSizeSelect.options[i].value == lcmSize) { lcmSizeOption = lcmSizeSelect.options[i]; break; }",
    "                }",
    "",
    "                if (!lcmSizeOption) {",
    "                    lcmSizeOption = lcmSizeSelect.querySelector('option[data-lcm-custom]');",
    "                    if (!lcmSizeOption) {",
    "                        lcmSizeOption = document.createElement('option');",
    "                        lcmSizeOption.setAttribute('data-lcm-custom', '1');",
    "                        lcmSizeSelect.appendChild(lcmSizeOption);",
    "                    }",
    "                    lcmSizeOption.value = lcmSize;",
    "                    lcmSizeOption.textContent = lcmSize + 'x (custom)';",
    "                }",
    "",
    "                lcmSizeSelect.value = lcmSize;",
    "            }",
    "",
    "            localStorage.setItem('canvasSize', lcmSize);",
    "            // lclite:control-panel",
    "        }",
    ""
   ],
   "note": "view/client.ejs @ old line 304",
   "occ": 1,
   "pos": [
    303,
    2,
    301,
    306
   ]
  },
  {
   "find": [
    "            canvas.style.imageRendering = filterSelect;",
    "        }",
    "    </script>",
    "</head>"
   ],
   "replace": [
    "            canvas.style.imageRendering = filterSelect;",
    "        }",
    "        <!-- lclite:control-panel -->",
    "",
    "    </script>",
    "</head>"
   ],
   "note": "view/client.ejs @ old line 331",
   "occ": 1,
   "pos": [
    331,
    0,
    329,
    332
   ]
  },
  {
   "find": [
    "    </script>",
    "",
    "    <script type=\"module\">",
    "        import { Client } from './client/client.js';"
   ],
   "replace": [
    "    </script>",
    "",
    "    <!-- lclite:control-panel -->",
    "    <!-- lclite control panel -->",
    "    <link rel=\"stylesheet\" href=\"/lclite/panel.css?v=6\">",
    "    <script src=\"/lclite/panel.js?v=6\" defer></script>",
    "",
    "    <script type=\"module\">",
    "        import { Client } from './client/client.js';"
   ],
   "note": "view/client.ejs @ old line 392",
   "occ": 1,
   "pos": [
    392,
    0,
    390,
    393
   ]
  }
 ]
}