{
 "file": "webclient/src/dash3d/World.ts",
 "mod": "camera",
 "generated_from": {
  "repo": "webclient",
  "head": "bd6cad7147cc9f44d5eb12de93f0736c18e1c4d0"
 },
 "hunks": [
  {
   "find": [
    "    private static visBackingDirty: boolean[][] | null = null;",
    ""
   ],
   "replace": [
    "    private static visBackingDirty: boolean[][] | null = null;",
    "    /// custom: camera zoom. visBacking is precomputed at the default camera distance, so",
    "    /// zooming out would blank tiles at the edge of the draw window. When zoomed we bypass",
    "    /// the table with a live per-tile frustum test (see visTile / setVisZoom).",
    "    private static visZoom: number = 1;",
    "    private static visSinX: number = 0;",
    "    private static visCosX: number = 0;",
    "    private static visSinY: number = 0;",
    "    private static visCosY: number = 0;",
    "    private static visFar: number = 3500;",
    "    private static visTanH: number = 0.5; // focal length is 512 (<<9): half-width 256 / 512",
    "    private static visTanV: number = 0.326; // half-height 167 / 512",
    "    /// custom: half-width of the square tile draw window around the camera (25 by default,",
    "    /// grows with zoom so distant ground still renders; capped at 63 tiles)",
    "    private static viewRadius: number = 25;",
    "",
    "    /// custom: call whenever the camera zoom multiplier changes (1 = default distance).",
    "    /// Zooming out scales camera distance, so the far clip plane and the ±25 tile draw",
    "    /// window must scale with it or the map edges blank out around the player.",
    "    static setVisZoom(zoom: number): void {",
    "        this.visZoom = zoom;",
    "        this.visFar = (3500 * zoom) | 0;",
    "        this.viewRadius = zoom > 1.05 ? Math.min(63, Math.ceil(25 * zoom)) : 25;",
    "        Pix3D.farPlane = this.visFar;",
    "    }",
    "",
    "    /// custom: live frustum probe for the tile at grid delta (dx, dz) from the camera tile,",
    "    /// ground height gh (absolute; Y grows downward in this engine). Camera-relative frame",
    "    /// matches testPoint(). Only used when zoomed out — the precomputed visBacking table is",
    "    /// baked at the default camera distance and would blank edge tiles when zoomed.",
    "    private static visTileAt(dx: number, dz: number, gh: number, eyeY: number): boolean {",
    "        if (this.visZoom <= 1.05) {",
    "            return false;",
    "        }",
    "",
    "        const sinX: number = this.visSinX;",
    "        const cosX: number = this.visCosX;",
    "        const sinY: number = this.visSinY;",
    "        const cosY: number = this.visCosY;",
    "        const far: number = this.visFar;",
    "        const tanH: number = this.visTanH;",
    "        const tanV: number = this.visTanV;",
    "        const baseY: number = gh - eyeY;",
    "",
    "        // sample the four tile corners across a vertical band (buildings above, pits below),",
    "        // mirroring the precompute's frustumStart/frustumEnd span",
    "        for (let i: number = 0; i < 4; i++) {",
    "            const px: number = (World.gx + dx + (i === 0 || i === 3 ? 0 : 1)) * 128 - World.cx;",
    "            const pz: number = (World.gz + dz + (i < 2 ? 0 : 1)) * 128 - World.cz;",
    "",
    "            const east: number = (pz * sinY + px * cosY) >> 16;",
    "            const north: number = (pz * cosY - px * sinY) >> 16;",
    "",
    "            for (let dy: number = -600; dy <= 800; dy += 350) {",
    "                const yy: number = baseY + dy;",
    "                const depth: number = (yy * sinX + north * cosX) >> 16;",
    "                if (depth < 50 || depth > far) {",
    "                    continue;",
    "                }",
    "                const height: number = (yy * cosX - north * sinX) >> 16;",
    "                if (east >= -depth * tanH && east <= depth * tanH && height >= -depth * tanV && height <= depth * tanV) {",
    "                    return true;",
    "                }",
    "            }",
    "        }",
    "",
    "        return false;",
    "    }",
    ""
   ],
   "note": "src/dash3d/World.ts @ old line 88",
   "occ": 1
  },
  {
   "find": [
    "        World.clickY = mouseY;",
    "        World.groundX = -1;",
    "        World.groundZ = -1;",
    "    }",
    "",
    "    renderAll(eyeX: number, eyeY: number, eyeZ: number, maxLevel: number, eyeYaw: number, eyePitch: number): void {",
    "        if (eyeX < 0) {",
    "            eyeX = 0;",
    "        } else if (eyeX >= this.maxTileX * 128) {",
    "            eyeX = this.maxTileX * 128 - 1;",
    "        }",
    "",
    "        if (eyeZ < 0) {",
    "            eyeZ = 0;",
    "        } else if (eyeZ >= this.maxTileZ * 128) {",
    "            eyeZ = this.maxTileZ * 128 - 1;",
    "        }",
    "",
    "        World.cycleNo++;",
    "        World.cameraSinX = Pix3D.sinTable[eyePitch];",
    "        World.cameraCosX = Pix3D.cosTable[eyePitch];",
    "        World.cameraSinY = Pix3D.sinTable[eyeYaw];",
    "        World.cameraCosY = Pix3D.cosTable[eyeYaw];",
    "",
    "        World.visBackingDirty = World.visBacking[((eyePitch - 128) / 32) | 0][(eyeYaw / 64) | 0];",
    "        World.cx = eyeX;",
    "        World.cy = eyeY;",
    "        World.cz = eyeZ;",
    "        World.gx = (eyeX / 128) | 0;",
    "        World.gz = (eyeZ / 128) | 0;",
    "        World.maxLevel = maxLevel;",
    "",
    "        World.minX = World.gx - 25;",
    "        if (World.minX < 0) {",
    "            World.minX = 0;",
    "        }",
    "",
    "        World.minZ = World.gz - 25;",
    "        if (World.minZ < 0) {",
    "            World.minZ = 0;",
    "        }",
    "",
    "        World.maxX = World.gx + 25;",
    "        if (World.maxX > this.maxTileX) {",
    "            World.maxX = this.maxTileX;",
    "        }",
    "",
    "        World.maxZ = World.gz + 25;",
    "        if (World.maxZ > this.maxTileZ) {",
    "            World.maxZ = this.maxTileZ;",
    "        }",
    "",
    "        this.calcOcclude();",
    "        World.fillLeft = 0;",
    "",
    "        for (let level: number = this.minLevel; level < this.maxTileLevel; level++) {",
    "            const tiles: (Square | null)[][] = this.squares[level];",
    "            for (let x: number = World.minX; x < World.maxX; x++) {",
    "                for (let z: number = World.minZ; z < World.maxZ; z++) {",
    "                    const tile: Square | null = tiles[x][z];",
    "                    if (!tile) {",
    "                        continue;",
    "                    }",
    "",
    "                    if (tile.drawLevel <= maxLevel && (World.visBackingDirty[x + 25 - World.gx][z + 25 - World.gz] || this.groundh[level][x][z] - eyeY >= 2000)) {",
    "                        tile.drawFront = true;",
    "                        tile.drawBack = true;",
    "                        tile.drawSprites = tile.spriteCount > 0;",
    "                        World.fillLeft++;",
    "                    } else {",
    "                        tile.drawFront = false;",
    "                        tile.drawBack = false;",
    "                        tile.cornerSides = 0;",
    "                    }",
    "                }",
    "            }",
    "        }",
    "",
    "        for (let level: number = this.minLevel; level < this.maxTileLevel; level++) {",
    "            const tiles: (Square | null)[][] = this.squares[level];",
    "            for (let dx: number = -25; dx <= 0; dx++) {",
    "                const rightTileX: number = World.gx + dx;",
    "                const leftTileX: number = World.gx - dx;",
    "",
    "                if (rightTileX < World.minX && leftTileX >= World.maxX) {",
    "                    continue;",
    "                }",
    "",
    "                for (let dz: number = -25; dz <= 0; dz++) {",
    "                    const forwardTileZ: number = World.gz + dz;",
    "                    const backwardTileZ: number = World.gz - dz;",
    "                    let tile: Square | null;",
    "",
    "                    if (rightTileX >= World.minX) {",
    "                        if (forwardTileZ >= World.minZ) {",
    "                            tile = tiles[rightTileX][forwardTileZ];",
    "                            if (tile && tile.drawFront) {",
    "                                this.fill(tile, true);",
    "                            }",
    "                        }",
    "",
    "                        if (backwardTileZ < World.maxZ) {",
    "                            tile = tiles[rightTileX][backwardTileZ];",
    "                            if (tile && tile.drawFront) {",
    "                                this.fill(tile, true);",
    "                            }",
    "                        }",
    "                    }",
    "",
    "                    if (leftTileX < World.maxX) {",
    "                        if (forwardTileZ >= World.minZ) {",
    "                            tile = tiles[leftTileX][forwardTileZ];",
    "                            if (tile && tile.drawFront) {",
    "                                this.fill(tile, true);",
    "                            }",
    "                        }",
    "",
    "                        if (backwardTileZ < World.maxZ) {",
    "                            tile = tiles[leftTileX][backwardTileZ];",
    "                            if (tile && tile.drawFront) {",
    "                                this.fill(tile, true);",
    "                            }",
    "                        }",
    "                    }",
    "",
    "                    if (World.fillLeft === 0) {",
    "                        World.click = false;",
    "                        return;",
    "                    }",
    "                }",
    "            }",
    "        }",
    "",
    "        for (let level: number = this.minLevel; level < this.maxTileLevel; level++) {",
    "            const tiles: (Square | null)[][] = this.squares[level];",
    "            for (let dx: number = -25; dx <= 0; dx++) {",
    "                const rightTileX: number = World.gx + dx;",
    "                const leftTileX: number = World.gx - dx;",
    "",
    "                if (rightTileX < World.minX && leftTileX >= World.maxX) {",
    "                    continue;",
    "                }",
    "",
    "                for (let dz: number = -25; dz <= 0; dz++) {",
    "                    const forwardTileZ: number = World.gz + dz;"
   ],
   "replace": [
    "        World.clickY = mouseY;",
    "        World.clickCycle = World.cycleNo;",
    "        World.groundX = -1;",
    "        World.groundZ = -1;",
    "    }",
    "",
    "    /// custom: disarm the ground pick once its destination has been consumed, so the walk",
    "    /// target stops following a stale click pixel when the camera rotates (arrow keys or",
    "    /// middle-drag). Without this the pick re-resolves every render frame forever.",
    "    static clearPick(): void {",
    "        World.click = false;",
    "    }",
    "",
    "    /// custom: cycle the pick was armed on, so an unresolved pick expires quickly",
    "    static clickCycle: number = 0;",
    "",
    "    renderAll(eyeX: number, eyeY: number, eyeZ: number, maxLevel: number, eyeYaw: number, eyePitch: number): void {",
    "        if (eyeX < 0) {",
    "            eyeX = 0;",
    "        } else if (eyeX >= this.maxTileX * 128) {",
    "            eyeX = this.maxTileX * 128 - 1;",
    "        }",
    "",
    "        if (eyeZ < 0) {",
    "            eyeZ = 0;",
    "        } else if (eyeZ >= this.maxTileZ * 128) {",
    "            eyeZ = this.maxTileZ * 128 - 1;",
    "        }",
    "",
    "        // custom: the ground pick stays armed after a walk click and re-resolves every",
    "        // frame (see updateMousePicking). gameLoop() disarms it once the destination is",
    "        // consumed; this covers picks that never resolve (clicked water/wall), so camera",
    "        // rotation can't make a stale click pixel \"discover\" a walk destination later.",
    "        if (World.click && World.groundX === -1 && World.cycleNo - World.clickCycle > 20) {",
    "            World.click = false;",
    "        }",
    "",
    "        World.cycleNo++;",
    "        World.cameraSinX = Pix3D.sinTable[eyePitch];",
    "        World.cameraCosX = Pix3D.cosTable[eyePitch];",
    "        World.cameraSinY = Pix3D.sinTable[eyeYaw];",
    "        World.cameraCosY = Pix3D.cosTable[eyeYaw];",
    "        // custom: cache trig for the zoomed live visibility probe",
    "        World.visSinX = World.cameraSinX;",
    "        World.visCosX = World.cameraCosX;",
    "        World.visSinY = World.cameraSinY;",
    "        World.visCosY = World.cameraCosY;",
    "",
    "        World.visBackingDirty = World.visBacking[((eyePitch - 128) / 32) | 0][(eyeYaw / 64) | 0];",
    "        World.cx = eyeX;",
    "        World.cy = eyeY;",
    "        World.cz = eyeZ;",
    "        World.gx = (eyeX / 128) | 0;",
    "        World.gz = (eyeZ / 128) | 0;",
    "        World.maxLevel = maxLevel;",
    "",
    "        World.minX = World.gx - World.viewRadius;",
    "        if (World.minX < 0) {",
    "            World.minX = 0;",
    "        }",
    "",
    "        World.minZ = World.gz - World.viewRadius;",
    "        if (World.minZ < 0) {",
    "            World.minZ = 0;",
    "        }",
    "",
    "        World.maxX = World.gx + World.viewRadius;",
    "        if (World.maxX > this.maxTileX) {",
    "            World.maxX = this.maxTileX;",
    "        }",
    "",
    "        World.maxZ = World.gz + World.viewRadius;",
    "        if (World.maxZ > this.maxTileZ) {",
    "            World.maxZ = this.maxTileZ;",
    "        }",
    "",
    "        this.calcOcclude();",
    "        World.fillLeft = 0;",
    "",
    "        for (let level: number = this.minLevel; level < this.maxTileLevel; level++) {",
    "            const tiles: (Square | null)[][] = this.squares[level];",
    "            for (let x: number = World.minX; x < World.maxX; x++) {",
    "                for (let z: number = World.minZ; z < World.maxZ; z++) {",
    "                    const tile: Square | null = tiles[x][z];",
    "                    if (!tile) {",
    "                        continue;",
    "                    }",
    "",
    "                    // custom: when zoomed out the precomputed table (and its ±25 window) is",
    "                    // invalid; consult the live frustum probe for every tile instead.",
    "                    const vdx: number = x - World.gx;",
    "                    const vdz: number = z - World.gz;",
    "                    const visible: boolean = World.visZoom > 1.05",
    "                        ? World.visTileAt(vdx, vdz, this.groundh[level][x][z], eyeY)",
    "                        : World.visBackingDirty![vdx + 25][vdz + 25];",
    "",
    "                    if (tile.drawLevel <= maxLevel && (visible || this.groundh[level][x][z] - eyeY >= 2000)) {",
    "                        tile.drawFront = true;",
    "                        tile.drawBack = true;",
    "                        tile.drawSprites = tile.spriteCount > 0;",
    "                        World.fillLeft++;",
    "                    } else {",
    "                        tile.drawFront = false;",
    "                        tile.drawBack = false;",
    "                        tile.cornerSides = 0;",
    "                    }",
    "                }",
    "            }",
    "        }",
    "",
    "        for (let level: number = this.minLevel; level < this.maxTileLevel; level++) {",
    "            const tiles: (Square | null)[][] = this.squares[level];",
    "            for (let dx: number = -World.viewRadius; dx <= 0; dx++) {",
    "                const rightTileX: number = World.gx + dx;",
    "                const leftTileX: number = World.gx - dx;",
    "",
    "                if (rightTileX < World.minX && leftTileX >= World.maxX) {",
    "                    continue;",
    "                }",
    "",
    "                for (let dz: number = -World.viewRadius; dz <= 0; dz++) {",
    "                    const forwardTileZ: number = World.gz + dz;",
    "                    const backwardTileZ: number = World.gz - dz;",
    "                    let tile: Square | null;",
    "",
    "                    if (rightTileX >= World.minX) {",
    "                        if (forwardTileZ >= World.minZ) {",
    "                            tile = tiles[rightTileX][forwardTileZ];",
    "                            if (tile && tile.drawFront) {",
    "                                this.fill(tile, true);",
    "                            }",
    "                        }",
    "",
    "                        if (backwardTileZ < World.maxZ) {",
    "                            tile = tiles[rightTileX][backwardTileZ];",
    "                            if (tile && tile.drawFront) {",
    "                                this.fill(tile, true);",
    "                            }",
    "                        }",
    "                    }",
    "",
    "                    if (leftTileX < World.maxX) {",
    "                        if (forwardTileZ >= World.minZ) {",
    "                            tile = tiles[leftTileX][forwardTileZ];",
    "                            if (tile && tile.drawFront) {",
    "                                this.fill(tile, true);",
    "                            }",
    "                        }",
    "",
    "                        if (backwardTileZ < World.maxZ) {",
    "                            tile = tiles[leftTileX][backwardTileZ];",
    "                            if (tile && tile.drawFront) {",
    "                                this.fill(tile, true);",
    "                            }",
    "                        }",
    "                    }",
    "",
    "                    if (World.fillLeft === 0) {",
    "                        World.click = false;",
    "                        return;",
    "                    }",
    "                }",
    "            }",
    "        }",
    "",
    "        for (let level: number = this.minLevel; level < this.maxTileLevel; level++) {",
    "            const tiles: (Square | null)[][] = this.squares[level];",
    "            for (let dx: number = -World.viewRadius; dx <= 0; dx++) {",
    "                const rightTileX: number = World.gx + dx;",
    "                const leftTileX: number = World.gx - dx;",
    "",
    "                if (rightTileX < World.minX && leftTileX >= World.maxX) {",
    "                    continue;",
    "                }",
    "",
    "                for (let dz: number = -World.viewRadius; dz <= 0; dz++) {",
    "                    const forwardTileZ: number = World.gz + dz;"
   ],
   "note": "src/dash3d/World.ts @ old line 921",
   "occ": 1
  },
  {
   "find": [
    "        const occluders: (Occlude | null)[] = World.occluders[World.maxLevel];",
    "",
    "        World.numActiveOccluders = 0;",
    "",
    "        for (let i: number = 0; i < count; i++) {",
    "            const occluder: Occlude | null = occluders[i];",
    "            if (!occluder) {",
    "                continue;",
    "            }",
    "",
    "            let deltaMaxY: number;",
    "            let deltaMinTileZ: number;",
    "            let deltaMaxTileZ: number;",
    "            let deltaMaxTileX: number;",
    "",
    "            if (occluder.type === 1) {",
    "                deltaMaxY = occluder.minTileX + 25 - World.gx;",
    "                if (deltaMaxY >= 0 && deltaMaxY <= 50) {",
    "                    deltaMinTileZ = occluder.minTileZ + 25 - World.gz;",
    "                    if (deltaMinTileZ < 0) {",
    "                        deltaMinTileZ = 0;",
    "                    }",
    "",
    "                    deltaMaxTileZ = occluder.maxTileZ + 25 - World.gz;",
    "                    if (deltaMaxTileZ > 50) {",
    "                        deltaMaxTileZ = 50;",
    "                    }",
    "",
    "                    let ok: boolean = false;",
    "                    while (deltaMinTileZ <= deltaMaxTileZ) {",
    "                        if (World.visBackingDirty && World.visBackingDirty[deltaMaxY][deltaMinTileZ++]) {",
    "                            ok = true;",
    "                            break;",
    "                        }",
    "                    }",
    "",
    "                    if (ok) {",
    "                        deltaMaxTileX = World.cx - occluder.minX;",
    "                        if (deltaMaxTileX > 32) {",
    "                            occluder.mode = 1;",
    "                        } else {",
    "                            if (deltaMaxTileX >= -32) {",
    "                                continue;",
    "                            }",
    "",
    "                            occluder.mode = 2;",
    "                            deltaMaxTileX = -deltaMaxTileX;",
    "                        }",
    "",
    "                        occluder.minDeltaZ = (((occluder.minZ - World.cz) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.maxDeltaZ = (((occluder.maxZ - World.cz) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.minDeltaY = (((occluder.minY - World.cy) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.maxDeltaY = (((occluder.maxY - World.cy) << 8) / deltaMaxTileX) | 0;",
    "                        World.activeOccluders[World.numActiveOccluders++] = occluder;",
    "                    }",
    "                }",
    "            } else if (occluder.type === 2) {",
    "                deltaMaxY = occluder.minTileZ + 25 - World.gz;",
    "",
    "                if (deltaMaxY >= 0 && deltaMaxY <= 50) {",
    "                    deltaMinTileZ = occluder.minTileX + 25 - World.gx;",
    "                    if (deltaMinTileZ < 0) {",
    "                        deltaMinTileZ = 0;",
    "                    }",
    "",
    "                    deltaMaxTileZ = occluder.maxTileX + 25 - World.gx;",
    "                    if (deltaMaxTileZ > 50) {",
    "                        deltaMaxTileZ = 50;",
    "                    }",
    "",
    "                    let ok: boolean = false;",
    "                    while (deltaMinTileZ <= deltaMaxTileZ) {",
    "                        if (World.visBackingDirty && World.visBackingDirty[deltaMinTileZ++][deltaMaxY]) {",
    "                            ok = true;",
    "                            break;",
    "                        }",
    "                    }",
    "",
    "                    if (ok) {",
    "                        deltaMaxTileX = World.cz - occluder.minZ;",
    "                        if (deltaMaxTileX > 32) {",
    "                            occluder.mode = 3;",
    "                        } else {",
    "                            if (deltaMaxTileX >= -32) {",
    "                                continue;",
    "                            }",
    "",
    "                            occluder.mode = 4;",
    "                            deltaMaxTileX = -deltaMaxTileX;",
    "                        }",
    "",
    "                        occluder.minDeltaX = (((occluder.minX - World.cx) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.maxDeltaX = (((occluder.maxX - World.cx) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.minDeltaY = (((occluder.minY - World.cy) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.maxDeltaY = (((occluder.maxY - World.cy) << 8) / deltaMaxTileX) | 0;",
    "                        World.activeOccluders[World.numActiveOccluders++] = occluder;",
    "                    }",
    "                }",
    "            } else if (occluder.type === 4) {",
    "                deltaMaxY = occluder.minY - World.cy;",
    "",
    "                if (deltaMaxY > 128) {",
    "                    deltaMinTileZ = occluder.minTileZ + 25 - World.gz;",
    "                    if (deltaMinTileZ < 0) {",
    "                        deltaMinTileZ = 0;",
    "                    }",
    "",
    "                    deltaMaxTileZ = occluder.maxTileZ + 25 - World.gz;",
    "                    if (deltaMaxTileZ > 50) {",
    "                        deltaMaxTileZ = 50;",
    "                    }",
    "",
    "                    if (deltaMinTileZ <= deltaMaxTileZ) {",
    "                        let deltaMinTileX: number = occluder.minTileX + 25 - World.gx;",
    "                        if (deltaMinTileX < 0) {",
    "                            deltaMinTileX = 0;",
    "                        }",
    "",
    "                        deltaMaxTileX = occluder.maxTileX + 25 - World.gx;",
    "                        if (deltaMaxTileX > 50) {",
    "                            deltaMaxTileX = 50;",
    "                        }",
    "",
    "                        let ok: boolean = false;",
    "                        find_visible_tile: for (let x: number = deltaMinTileX; x <= deltaMaxTileX; x++) {",
    "                            for (let z: number = deltaMinTileZ; z <= deltaMaxTileZ; z++) {",
    "                                if (World.visBackingDirty && World.visBackingDirty[x][z]) {",
    "                                    ok = true;"
   ],
   "replace": [
    "        const occluders: (Occlude | null)[] = World.occluders[World.maxLevel];",
    "        // custom: when zoomed out, the precomputed table is invalid; probe live instead",
    "        const zoomed: boolean = World.visZoom > 1.05;",
    "",
    "        World.numActiveOccluders = 0;",
    "",
    "        for (let i: number = 0; i < count; i++) {",
    "            const occluder: Occlude | null = occluders[i];",
    "            if (!occluder) {",
    "                continue;",
    "            }",
    "",
    "            let deltaMaxY: number;",
    "            let deltaMinTileZ: number;",
    "            let deltaMaxTileZ: number;",
    "            let deltaMaxTileX: number;",
    "",
    "            if (occluder.type === 1) {",
    "                deltaMaxY = occluder.minTileX + 25 - World.gx;",
    "                if (deltaMaxY >= 0 && deltaMaxY <= 50) {",
    "                    deltaMinTileZ = occluder.minTileZ + 25 - World.gz;",
    "                    if (deltaMinTileZ < 0) {",
    "                        deltaMinTileZ = 0;",
    "                    }",
    "",
    "                    deltaMaxTileZ = occluder.maxTileZ + 25 - World.gz;",
    "                    if (deltaMaxTileZ > 50) {",
    "                        deltaMaxTileZ = 50;",
    "                    }",
    "",
    "                    let ok: boolean = false;",
    "                    if (zoomed) {",
    "                        // custom: check the occluder's tile row against the live probe",
    "                        const rowX: number = deltaMaxY - 25 + World.gx;",
    "                        for (let z: number = deltaMinTileZ - 25 + World.gz; z <= deltaMaxTileZ - 25 + World.gz; z++) {",
    "                            if (z >= 0 && z < 104 && rowX >= 0 && rowX < 104 && World.visTileAt(rowX - World.gx, z - World.gz, this.groundh[0][rowX][z], World.cy)) {",
    "                                ok = true;",
    "                                break;",
    "                            }",
    "                        }",
    "                    } else {",
    "                        while (deltaMinTileZ <= deltaMaxTileZ) {",
    "                            if (World.visBackingDirty && World.visBackingDirty[deltaMaxY][deltaMinTileZ++]) {",
    "                                ok = true;",
    "                                break;",
    "                            }",
    "                        }",
    "                    }",
    "",
    "                    if (ok) {",
    "                        deltaMaxTileX = World.cx - occluder.minX;",
    "                        if (deltaMaxTileX > 32) {",
    "                            occluder.mode = 1;",
    "                        } else {",
    "                            if (deltaMaxTileX >= -32) {",
    "                                continue;",
    "                            }",
    "",
    "                            occluder.mode = 2;",
    "                            deltaMaxTileX = -deltaMaxTileX;",
    "                        }",
    "",
    "                        occluder.minDeltaZ = (((occluder.minZ - World.cz) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.maxDeltaZ = (((occluder.maxZ - World.cz) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.minDeltaY = (((occluder.minY - World.cy) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.maxDeltaY = (((occluder.maxY - World.cy) << 8) / deltaMaxTileX) | 0;",
    "                        World.activeOccluders[World.numActiveOccluders++] = occluder;",
    "                    }",
    "                }",
    "            } else if (occluder.type === 2) {",
    "                deltaMaxY = occluder.minTileZ + 25 - World.gz;",
    "",
    "                if (deltaMaxY >= 0 && deltaMaxY <= 50) {",
    "                    deltaMinTileZ = occluder.minTileX + 25 - World.gx;",
    "                    if (deltaMinTileZ < 0) {",
    "                        deltaMinTileZ = 0;",
    "                    }",
    "",
    "                    deltaMaxTileZ = occluder.maxTileX + 25 - World.gx;",
    "                    if (deltaMaxTileZ > 50) {",
    "                        deltaMaxTileZ = 50;",
    "                    }",
    "",
    "                    let ok: boolean = false;",
    "                    if (zoomed) {",
    "                        // custom: check the occluder's tile column against the live probe",
    "                        const colZ: number = deltaMaxY - 25 + World.gz;",
    "                        for (let x: number = deltaMinTileZ - 25 + World.gx; x <= deltaMaxTileZ - 25 + World.gx; x++) {",
    "                            if (x >= 0 && x < 104 && colZ >= 0 && colZ < 104 && World.visTileAt(x - World.gx, colZ - World.gz, this.groundh[0][x][colZ], World.cy)) {",
    "                                ok = true;",
    "                                break;",
    "                            }",
    "                        }",
    "                    } else {",
    "                        while (deltaMinTileZ <= deltaMaxTileZ) {",
    "                            if (World.visBackingDirty && World.visBackingDirty[deltaMinTileZ++][deltaMaxY]) {",
    "                                ok = true;",
    "                                break;",
    "                            }",
    "                        }",
    "                    }",
    "",
    "                    if (ok) {",
    "                        deltaMaxTileX = World.cz - occluder.minZ;",
    "                        if (deltaMaxTileX > 32) {",
    "                            occluder.mode = 3;",
    "                        } else {",
    "                            if (deltaMaxTileX >= -32) {",
    "                                continue;",
    "                            }",
    "",
    "                            occluder.mode = 4;",
    "                            deltaMaxTileX = -deltaMaxTileX;",
    "                        }",
    "",
    "                        occluder.minDeltaX = (((occluder.minX - World.cx) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.maxDeltaX = (((occluder.maxX - World.cx) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.minDeltaY = (((occluder.minY - World.cy) << 8) / deltaMaxTileX) | 0;",
    "                        occluder.maxDeltaY = (((occluder.maxY - World.cy) << 8) / deltaMaxTileX) | 0;",
    "                        World.activeOccluders[World.numActiveOccluders++] = occluder;",
    "                    }",
    "                }",
    "            } else if (occluder.type === 4) {",
    "                deltaMaxY = occluder.minY - World.cy;",
    "",
    "                if (deltaMaxY > 128) {",
    "                    deltaMinTileZ = occluder.minTileZ + 25 - World.gz;",
    "                    if (deltaMinTileZ < 0) {",
    "                        deltaMinTileZ = 0;",
    "                    }",
    "",
    "                    deltaMaxTileZ = occluder.maxTileZ + 25 - World.gz;",
    "                    if (deltaMaxTileZ > 50) {",
    "                        deltaMaxTileZ = 50;",
    "                    }",
    "",
    "                    if (deltaMinTileZ <= deltaMaxTileZ) {",
    "                        let deltaMinTileX: number = occluder.minTileX + 25 - World.gx;",
    "                        if (deltaMinTileX < 0) {",
    "                            deltaMinTileX = 0;",
    "                        }",
    "",
    "                        deltaMaxTileX = occluder.maxTileX + 25 - World.gx;",
    "                        if (deltaMaxTileX > 50) {",
    "                            deltaMaxTileX = 50;",
    "                        }",
    "",
    "                        let ok: boolean = false;",
    "                        find_visible_tile: for (let x: number = deltaMinTileX; x <= deltaMaxTileX; x++) {",
    "                            for (let z: number = deltaMinTileZ; z <= deltaMaxTileZ; z++) {",
    "                                // custom: live probe when zoomed (table stale), else cached row",
    "                                if (zoomed) {",
    "                                    const tx: number = x - 25 + World.gx;",
    "                                    const tz: number = z - 25 + World.gz;",
    "                                    if (tx >= 0 && tx < 104 && tz >= 0 && tz < 104 && World.visTileAt(tx - World.gx, tz - World.gz, this.groundh[0][tx][tz], World.cy)) {",
    "                                        ok = true;",
    "                                        break find_visible_tile;",
    "                                    }",
    "                                } else if (World.visBackingDirty && World.visBackingDirty[x][z]) {",
    "                                    ok = true;"
   ],
   "note": "src/dash3d/World.ts @ old line 1213",
   "occ": 1
  }
 ]
}