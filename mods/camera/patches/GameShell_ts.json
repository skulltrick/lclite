{
 "file": "webclient/src/client/GameShell.ts",
 "mod": "camera",
 "generated_from": {
  "repo": "webclient",
  "head": "bd6cad7147cc9f44d5eb12de93f0736c18e1c4d0"
 },
 "hunks": [
  {
   "find": [
    "    protected keyQueueWritePos: number = 0;",
    "",
    "    /// custom",
    "    protected resizeToFit: boolean = false;"
   ],
   "replace": [
    "    protected keyQueueWritePos: number = 0;",
    "",
    "    /// custom: osrs-style mouse wheel zoom + middle-button camera rotate lclite:camera",
    "    public middleMouseDown: boolean = false;",
    "",
    "    /// custom",
    "    protected resizeToFit: boolean = false;"
   ],
   "note": "src/client/GameShell.ts @ old line 37",
   "occ": 1,
   "ctx": 4
  },
  {
   "find": [
    "    };",
    "",
    "    protected async maininit() { }",
    "    protected mainquit() { }"
   ],
   "replace": [
    "    };",
    "",
    "    /// custom: wheel zoom mod switch (page scroll is only blocked while enabled) lclite:camera",
    "    public wheelZoomEnabled: boolean = true;",
    "    /// custom: middle-mouse-drag camera rotation mod switch",
    "    public middleRotateEnabled: boolean = true;",
    "    private readonly wheelHandler = (e: WheelEvent): void => {",
    "        this.onwheel(e);",
    "    };",
    "",
    "    protected async maininit() { }",
    "    protected mainquit() { }"
   ],
   "note": "src/client/GameShell.ts @ old line 53",
   "occ": 1,
   "ctx": 4
  },
  {
   "find": [
    "        }",
    "",
    "        // Preventing mouse events from bubbling up to the context menu in the browser for our canvas.",
    "        // This may need to be hooked up to our own context menu in the future."
   ],
   "replace": [
    "        }",
    "",
    "        // custom: mouse wheel zoom (non-passive so we can block page scroll while enabled) lclite:camera",
    "        canvas.addEventListener('wheel', this.wheelHandler, { passive: false });",
    "",
    "        // Preventing mouse events from bubbling up to the context menu in the browser for our canvas.",
    "        // This may need to be hooked up to our own context menu in the future."
   ],
   "note": "src/client/GameShell.ts @ old line 111",
   "occ": 1,
   "ctx": 4
  },
  {
   "find": [
    "    protected mouseDown(x: number, y: number, e: MouseEvent) {",
    "        this.idleTimer = performance.now();",
    "        this.nextMouseClickX = x;",
    "        this.nextMouseClickY = y;"
   ],
   "replace": [
    "    protected mouseDown(x: number, y: number, e: MouseEvent) {",
    "        this.idleTimer = performance.now();",
    "        // lclite:camera",
    "",
    "        // custom: middle mouse button = camera rotate drag, never a game click",
    "        if (e.button === 1 && this.middleRotateEnabled) {",
    "            this.middleMouseDown = true;",
    "            this.rotateLastX = x;",
    "            this.rotateLastY = y;",
    "            e.preventDefault();",
    "            return;",
    "        }",
    "",
    "        this.nextMouseClickX = x;",
    "        this.nextMouseClickY = y;"
   ],
   "note": "src/client/GameShell.ts @ old line 318",
   "occ": 1,
   "ctx": 4
  },
  {
   "find": [
    "    }",
    "",
    "    private onpointerdown(e: PointerEvent) {",
    "        if (e.clientX < 0 || e.clientY < 0) {"
   ],
   "replace": [
    "    }",
    "",
    "    /// custom: wheel + middle-drag camera rotate hooks (implemented by Client) lclite:camera",
    "    protected onwheel(_e: WheelEvent) { }",
    "    protected rotateLastX: number = 0;",
    "    protected rotateLastY: number = 0;",
    "",
    "    private onpointerdown(e: PointerEvent) {",
    "        if (e.clientX < 0 || e.clientY < 0) {"
   ],
   "note": "src/client/GameShell.ts @ old line 335",
   "occ": 1,
   "ctx": 4
  },
  {
   "find": [
    "    protected mouseUp(x: number, y: number, e: MouseEvent) {",
    "        this.idleTimer = performance.now();",
    "        this.mouseButton = 0;",
    ""
   ],
   "replace": [
    "    protected mouseUp(x: number, y: number, e: MouseEvent) {",
    "        this.idleTimer = performance.now();",
    "        // custom: middle button release ends camera rotation drag lclite:camera",
    "        if (e.button === 1) {",
    "            this.middleMouseDown = false;",
    "        }",
    "        this.mouseButton = 0;",
    ""
   ],
   "note": "src/client/GameShell.ts @ old line 356",
   "occ": 1,
   "ctx": 4
  },
  {
   "find": [
    "        this.nextMouseClickButton = 0;",
    "        this.mouseButton = 0;",
    "    }",
    ""
   ],
   "replace": [
    "        this.nextMouseClickButton = 0;",
    "        this.mouseButton = 0;",
    "        // custom: stop rotating if the mouse leaves the canvas mid-drag lclite:camera",
    "        this.middleMouseDown = false;",
    "    }",
    ""
   ],
   "note": "src/client/GameShell.ts @ old line 401",
   "occ": 1,
   "ctx": 4
  }
 ]
}