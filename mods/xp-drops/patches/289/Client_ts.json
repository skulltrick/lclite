{
 "file": "webclient/src/client/Client.ts",
 "mod": "xp-drops",
 "generated_from": {
  "repo": "webclient",
  "head": "a52b321a9f42530f3dd2f1a02cd335c955791a12"
 },
 "hunks": [
  {
   "find": [
    "const SCROLLBAR_GRIP_LOWLIGHT = 0x332d25;",
    "",
    "export class Client extends GameShell {",
    "    static nodeId: number = 10;"
   ],
   "replace": [
    "const SCROLLBAR_GRIP_LOWLIGHT = 0x332d25;",
    "",
    "/* ─── custom (lclite \"xp-drops\" mod) tunables ──────────────────────────────── lclite:xp-drops",
    "   Rows AND tracker panel render straight into the areaGame game buffer (512x334 ==",
    "   the viewport rect (4,4)-(516,338) of the 765x503 frame), hooked at the top of",
    "   entityOverlays() — so the crosshair, private messages, modals and the",
    "   right-click minimenu all land ON TOP of the tracker later in the same buffer,",
    "   exactly like OSRS. It never floats over an interface, and opening a menu no",
    "   longer blanks it.",
    "   Layout mirrors OSRS defaults: tracker in the viewport's top-right corner,",
    "   rows scroll up and retire just below it. */",
    "const XP_DROP_X = 508;            // right edge of drop rows (buffer x; flush with panel text)",
    "const XP_DROP_BOTTOM = 276;       // y where a fresh drop appears (bottom of band)",
    "const XP_DROP_TOP = 56;           // y where rows retire (baseline; just below the panel)",
    "const XP_DROP_STEP = 15;          // min vertical gap between rows",
    "const XP_DROP_SPEED = 1.0;        // px per client cycle upward (loopCycle ~52/s -> ~4.2s life, < XP_HIDE_MS)",
    "const XP_PANEL_GX = 362;          // panel origin, buffer space (viewport top-right: 512 - XP_PANEL_W, 2)",
    "const XP_PANEL_GY = 2;",
    "const XP_PANEL_W = 150;",
    "const XP_PANEL_H = 40;",
    "// alt-drag placement: anchor id -> (x,y) fractions of the 512x334 viewport buffer. lclite:xp-drops",
    "// The panel drag layer writes lcmXpTrackerAnchor/Offset (buffer px); we read our",
    "// own keys per frame here (rule 5) and derive the drop band from the panel edges.",
    "// POSITIONAL, not an object keyed by the anchor name: terser's property mangler renames",
    "// object-literal keys, so `XP_ANCH['MC']` shipped as `YG[\"MC\"]` against a renamed `{av:…}`",
    "// and every lookup missed — the tracker could not be placed at all. Names and fractions",
    "// are parallel arrays, matched by index.",
    "const XP_ANCH_NAMES: string[] = ['TL', 'TC', 'TR', 'ML', 'MC', 'MR', 'BL', 'BC', 'BR'];",
    "const XP_ANCH: number[][] = [[0, 0], [0.5, 0], [1, 0], [0, 0.5], [0.5, 0.5], [1, 0.5], [0, 1], [0.5, 1], [1, 1]];",
    "const XP_PANEL_BG = 0x8a7454;     // lightish 2004Scape tan (matches sidebar backbase)",
    "const XP_PANEL_BORDER = 0x4e3f27; // dark brown outline",
    "const XP_PANEL_BAR_BG = 0x33281a; // progress-bar groove",
    "const XP_HIDE_MS = 6000;          // auto-hide after this long without a gain (OSRS-style)",
    "const XP_BURST_MS = 1000;         // gains this close merge into one tracked burst (see xpRates)",
    "const XP_MAX_DROPS = 28;",
    "/**/",
    "",
    "export class Client extends GameShell {",
    "    static nodeId: number = 10;"
   ],
   "note": "src/client/Client.ts @ old line 87",
   "occ": 1,
   "pos": [
    87,
    0,
    85,
    88
   ]
  },
  {
   "find": [
    "    static readbit = new Int32Array(32);",
    "    static levelExperience: number[] = [];",
    "",
    "    static {"
   ],
   "replace": [
    "    static readbit = new Int32Array(32);",
    "    static levelExperience: number[] = [];",
    "    /// custom (lclite \"xp-drops\"): skill id -> staticons sprite index. Pack order is lclite:xp-drops",
    "    /// NOT skill order — it is the stats.if grid order (attack,strength,defence,",
    "    /// ranged,prayer,magic,hitpoints,agility,herblore,thieving,crafting,fletching,",
    "    /// mining,smithing,fishing,cooking,firemaking,woodcutting); index 18 =",
    "    /// staticons2#0 (runecraft); -1 = skill has no icon in this pack (slayer,farming)",
    "    static readonly STAT_ICON_BY_SKILL: number[] = [0, 2, 1, 6, 3, 4, 5, 15, 17, 11, 14, 16, 10, 13, 12, 8, 7, 9, -1, -1, 18, -1, -1, -1, -1];",
    "",
    "    static {"
   ],
   "note": "src/client/Client.ts @ old line 121",
   "occ": 1,
   "pos": [
    121,
    0,
    119,
    122
   ]
  },
  {
   "find": [
    "    private statBaseLevel: Int32Array = new Int32Array(Skill.count);",
    "    private statXP: Int32Array = new Int32Array(Skill.count);",
    "",
    "    private oneMouseButton: number = 0;"
   ],
   "replace": [
    "    private statBaseLevel: Int32Array = new Int32Array(Skill.count);",
    "    private statXP: Int32Array = new Int32Array(Skill.count);",
    "    /// custom (lclite \"xp-drops\" mod): OSRS-style xp drops + level progress panel. lclite:xp-drops",
    "    /// Ported from the build-225 community guide, re-anchored and icon-mapped for 289.",
    "    public xpDropsEnabled: boolean = true;",
    "    private xpDrops: { skill: number; xp: number; y: number }[] = [];",
    "    private xpLastSkill: number = -1;",
    "    private xpLastGainAt: number = 0;   // performance.now() of the last real gain (auto-hide)",
    "    private xpRates: Int32Array = new Int32Array(Skill.count); // xp per skill inside the current burst",
    "    private xpStaticons: (Pix8 | null)[] = new Array(19).fill(null);",
    "    /// custom (lclite \"xp-drops\"): alt-drag placement — live panel origin + drop lclite:xp-drops",
    "    /// band, recomputed per frame in xpAnchorFrame() (defaults when no anchor set)",
    "    private xpPanelGX: number = XP_PANEL_GX;",
    "    private xpPanelGY: number = XP_PANEL_GY;",
    "    private xpDropX: number = XP_DROP_X;",
    "    private xpDropTop: number = XP_DROP_TOP;",
    "    private xpDropBottom: number = XP_DROP_BOTTOM;",
    "    private xpApiBound: boolean = false;",
    "",
    "    private oneMouseButton: number = 0;"
   ],
   "note": "src/client/Client.ts @ old line 422",
   "occ": 1,
   "pos": [
    422,
    0,
    420,
    423
   ]
  },
  {
   "find": [
    "            }",
    "",
    "            this.compass = Pix32.depack(media, 'compass', 0);",
    ""
   ],
   "replace": [
    "            }",
    "",
    "            // custom: skill icons for xp-drops (staticons = skills 0-17 in the SAME lclite:xp-drops",
    "            // slot order the stats panel uses, staticons2#0 = runecraft)",
    "            for (let i: number = 0; i < 18; i++) {",
    "                this.xpStaticons[i] = Pix8.depack(media, 'staticons', i);",
    "            }",
    "            this.xpStaticons[18] = Pix8.depack(media, 'staticons2', 0);",
    "",
    "            this.compass = Pix32.depack(media, 'compass', 0);",
    ""
   ],
   "note": "src/client/Client.ts @ old line 1015",
   "occ": 1,
   "pos": [
    1015,
    0,
    1013,
    1016
   ]
  },
  {
   "find": [
    "                this.idleTimer = performance.now();",
    "",
    "                for (let i: number = 0; i < 100; i++) {",
    "                    this.chatText[i] = null;"
   ],
   "replace": [
    "                this.idleTimer = performance.now();",
    "",
    "                // custom: fresh xp-drop feed per session (world hop / re-login). lclite:xp-drops",
    "                // statXP = -1 sentinel: the login UPDATE_STAT burst adopts silently",
    "                // (the server re-syncs every stat, lastStats.fill(-1) on Player init)",
    "                this.xpDrops.length = 0;",
    "                this.xpLastSkill = -1;",
    "                this.xpLastGainAt = 0;",
    "                this.xpRates.fill(0);",
    "                this.statXP.fill(-1);",
    "",
    "                for (let i: number = 0; i < 100; i++) {",
    "                    this.chatText[i] = null;"
   ],
   "note": "src/client/Client.ts @ old line 1820",
   "occ": 1,
   "pos": [
    1820,
    0,
    1818,
    1821
   ]
  },
  {
   "find": [
    "    }",
    "",
    "    private entityOverlays(): void {",
    "        this.chatCount = 0;"
   ],
   "replace": [
    "    }",
    "",
    "    /// custom (lclite \"xp-drops\" mod): \"Skill +N\" rows drifting up from the bottom of lclite:xp-drops",
    "    /// the band, one XP_DROP_SPEED per drawn frame; queued rows park below the",
    "    /// previous one so bursts stack instead of overlapping (OSRS behaviour).",
    "    private drawXpDrops(): void {",
    "        let lastY: number = -Infinity;",
    "        for (let i: number = 0; i < this.xpDrops.length; i++) {",
    "            const drop = this.xpDrops[i];",
    "            // frame-driven (not loopCycle-driven: loopCycle catches up in bursts",
    "            // during loading screens and would teleport fresh drops to their death).",
    "            // Array order = oldest first = highest row; each newer row must sit at",
    "            // least STEP below the previous, so crowded bursts wait below the band",
    "            // and slide in as the old rows retire (OSRS queue behaviour).",
    "            drop.y -= XP_DROP_SPEED;",
    "            if (drop.y < lastY + XP_DROP_STEP) {",
    "                drop.y = lastY + XP_DROP_STEP;",
    "            }",
    "            lastY = drop.y;",
    "",
    "            if (drop.y < this.xpDropTop) {",
    "                this.xpDrops.splice(i, 1);",
    "                i--;",
    "                continue;",
    "            }",
    "",
    "            if (drop.y <= this.xpDropBottom) {",
    "                const icon: Pix8 | null = this.xpDropIcon(drop.skill);",
    "                const text: string = `+${drop.xp.toLocaleString('en')}`;",
    "                const tw: number = this.p11?.stringWid(text) ?? 0;",
    "                if (icon) {",
    "                    // icon left of the number, both inside the right-aligned row",
    "                    const gap: number = 4;",
    "                    const iw: number = Math.min(icon.wi, 20);",
    "                    icon.plotSprite(this.xpDropX - gap - tw - iw, (drop.y | 0) - 12);",
    "                    this.p11?.drawStringRight(text, this.xpDropX, drop.y | 0, Colour.WHITE, true);",
    "                } else {",
    "                    // no icon in this cache pack (slayer/farming): keep the name",
    "                    this.p11?.drawStringRight(`${this.xpSkillLabel(drop.skill)} ${text}`, this.xpDropX, drop.y | 0, Colour.WHITE, true);",
    "                }",
    "            }",
    "        }",
    "    }",
    "",
    "    /// custom: the sprite for a skill's drop/panel icon (stat icons use the",
    "    /// original RS2 size; shrink anything larger so rows stay compact)",
    "    private xpDropIcon(skill: number): Pix8 | null {",
    "        const idx: number = Client.STAT_ICON_BY_SKILL[skill];",
    "        return idx >= 0 ? this.xpStaticons[idx] ?? null : null;",
    "    }",
    "",
    "    /// custom: Skill.names is lowercase ('hitpoints'); make it fit for the HUD",
    "    private xpSkillLabel(skill: number): string {",
    "        const n: string = Skill.names[skill] ?? '?';",
    "        return n.charAt(0).toUpperCase() + n.substring(1);",
    "    }",
    "",
    "    /// custom (lclite \"xp-drops\"): alt-drag placement math, run every frame at lclite:xp-drops",
    "    /// our own hook (rule 5): read our OWN lcmXpTracker* keys (only the panel",
    "    /// drag layer writes them), map 9-anchor + buffer-px offset onto the 512x334",
    "    /// viewport, clamp the whole panel inside it, and derive the drop band from",
    "    /// the panel edges (rows: right edge = panel right - 4, band top = below the",
    "    /// panel, bottom = +220 clamped). Then publish the window bridge the panel",
    "    /// hit-tests: lcmXpBounds() -> positional [x,y,w,h] or null (terser-proof).",
    "    private xpAnchorFrame(): void {",
    "        const ai: number = XP_ANCH_NAMES.indexOf(localStorage.getItem('lcmXpTrackerAnchor') || '');",
    "        if (ai >= 0) {",
    "            const a: number[] = XP_ANCH[ai];",
    "            const o: string[] = (localStorage.getItem('lcmXpTrackerOffset') || '0,0').split(',');",
    "            this.xpPanelGX = Math.round(512 * a[0]) + (parseInt(o[0]) || 0);",
    "            this.xpPanelGY = Math.round(334 * a[1]) + (parseInt(o[1]) || 0);",
    "        } else {",
    "            this.xpPanelGX = XP_PANEL_GX;",
    "            this.xpPanelGY = XP_PANEL_GY;",
    "        }",
    "        this.xpPanelGX = Math.max(0, Math.min(this.xpPanelGX, 512 - XP_PANEL_W));",
    "        this.xpPanelGY = Math.max(0, Math.min(this.xpPanelGY, 334 - XP_PANEL_H));",
    "        this.xpDropX = this.xpPanelGX + XP_PANEL_W - 4;",
    "        this.xpDropTop = this.xpPanelGY + XP_PANEL_H + 14;",
    "        this.xpDropBottom = Math.min(330, this.xpDropTop + 220);",
    "        const W: any = window as any;",
    "        if (!this.xpApiBound && W['lcmAnchor']) {",
    "            this.xpApiBound = true;   // panel present once = bridge stays live forever",
    "            W['lcmXpBounds'] = (): number[] | null =>",
    "                this.xpDropsEnabled ? [this.xpPanelGX, this.xpPanelGY, XP_PANEL_W, XP_PANEL_H] : null;",
    "            W['lcmAnchor']['registerCanvas']?.(['xp-tracker', 'lcmXpTrackerAnchor', 'lcmXpTrackerOffset', 'TR', '-152,2', () => W['lcmXpBounds'] ? W['lcmXpBounds']() : null]);",
    "        }",
    "    }",
    "",
    "    /// custom: level-progress panel — drawn straight into the game buffer (Pix2D",
    "    /// is bound to areaGame at the hook site). 2004Scape-tan palette (backbase",
    "    /// family); solid fill; rows retire at the live band top, just under the panel.",
    "    /// The crosshair/messages/modals/minimenu draw AFTER this in otherOverlays(),",
    "    /// which is why a right click no longer blanks the tracker.",
    "    private drawXpPanel(): void {",
    "        // alt-held (drag preview): paint the box even before any xp this session",
    "        Pix2D.fillRect(this.xpPanelGX, this.xpPanelGY, XP_PANEL_W, XP_PANEL_H, XP_PANEL_BG);",
    "        Pix2D.drawRect(this.xpPanelGX, this.xpPanelGY, XP_PANEL_W, XP_PANEL_H, XP_PANEL_BORDER);",
    "",
    "        const skill: number = this.xpLastSkill;",
    "        if (skill < 0) {",
    "            return;",
    "        }",
    "",
    "        const icon: number = Client.STAT_ICON_BY_SKILL[skill];",
    "        if (icon >= 0 && this.xpStaticons[icon]) {",
    "            this.xpStaticons[icon]!.plotSprite(this.xpPanelGX + 2, this.xpPanelGY + 2);",
    "        }",
    "",
    "        const total: number = this.statXP[skill];",
    "        const level: number = this.statBaseLevel[skill];",
    "        let prog: number = 1;",
    "        if (level < 99) {",
    "            // levelExperience[i] = total xp required for level i+2",
    "            const cur: number = level > 1 ? Client.levelExperience[level - 2] : 0;",
    "            const next: number = Client.levelExperience[level - 1];",
    "            prog = Math.min(1, Math.max(0, (total - cur) / (next - cur)));",
    "        }",
    "        const pct: number = (prog * 1000 / 10) | 0;",
    "",
    "        this.p11?.drawStringRight(`Experience  ${total.toLocaleString('en')}`, this.xpPanelGX + XP_PANEL_W - 4, this.xpPanelGY + 13, Colour.WHITE, true);",
    "        this.p11?.drawStringRight(`Level ${level} (${(pct / 10).toFixed(1)}%)`, this.xpPanelGX + XP_PANEL_W - 4, this.xpPanelGY + 26, Colour.WHITE, true);",
    "",
    "        const barX: number = this.xpPanelGX + 2;",
    "        const barY: number = this.xpPanelGY + XP_PANEL_H - 8;",
    "        const barW: number = XP_PANEL_W - 4;",
    "        Pix2D.fillRect(barX, barY, barW, 6, XP_PANEL_BAR_BG);",
    "        const r: number = ((1 - prog) * 255) | 0;",
    "        const g: number = (prog * 255) | 0;",
    "        Pix2D.fillRect(barX + 1, barY + 1, Math.max(0, (prog * barW) | 0) - 2, 4, (r << 16) | (g << 8));",
    "    }",
    "",
    "    private entityOverlays(): void {",
    "        this.chatCount = 0;"
   ],
   "note": "src/client/Client.ts @ old line 4572",
   "occ": 1,
   "pos": [
    4572,
    0,
    4570,
    4573
   ]
  },
  {
   "find": [
    "        this.chatCount = 0;",
    "",
    "        for (let index: number = -1; index < this.playerCount + this.npcCount; index++) {",
    "            let entity: ClientEntity | null = null;"
   ],
   "replace": [
    "        this.chatCount = 0;",
    "",
    "        // custom (lclite \"xp-drops\"): tracker panel + drop rows into the game buffer. lclite:xp-drops",
    "        // Drawn here (Pix2D bound to areaGame, right after the world render) so every",
    "        // interface composited later in the frame — entity chat, crosshair, private",
    "        // messages, modals, the right-click minimenu — paints OVER the tracker. This",
    "        // replaces the old canvas-space composite + blanket !isMenuOpen hide that",
    "        // blanked the tracker on every right click. Auto-hides XP_HIDE_MS after the",
    "        // last gained xp (OSRS-style; the rows drain at ~4.2s, before it goes).",
    "        // Reads our OWN key here (runs every frame) — no dependency on any hub.",
    "        this.xpDropsEnabled = localStorage.getItem('xpDrops') !== 'false';",
    "        this.xpAnchorFrame();   // lclite:xp-drops alt-drag placement from own keys",
    "        const xpW: any = window as any;",
    "        const xpAlt = xpW['lcmAlt'] === true;            // Alt held: show the grab box",
    "        const xpHeld = xpW['lcmHeld'] === 'xp-tracker';   // we own the drag: keep painting",
    "        if (this.xpDropsEnabled && (this.xpLastSkill >= 0 || xpAlt)) {",
    "            if (xpHeld || xpAlt || performance.now() - this.xpLastGainAt < XP_HIDE_MS) {",
    "                this.drawXpPanel();",
    "                this.drawXpDrops();",
    "            } else {",
    "                // rows still in flight when the window closed must not freeze",
    "                // mid-band and reappear on the next gain",
    "                this.xpDrops.length = 0;",
    "            }",
    "        }",
    "",
    "        for (let index: number = -1; index < this.playerCount + this.npcCount; index++) {",
    "            let entity: ClientEntity | null = null;"
   ],
   "note": "src/client/Client.ts @ old line 4575",
   "occ": 1,
   "pos": [
    4575,
    0,
    4573,
    4576
   ]
  },
  {
   "find": [
    "                const level: number = this.in.g1();",
    "",
    "                this.statXP[stat] = xp;",
    "                this.statEffectiveLevel[stat] = level;"
   ],
   "replace": [
    "                const level: number = this.in.g1();",
    "",
    "                // custom: a gained amount (xp > stored) = drop; equal/below = login lclite:xp-drops",
    "                // sync or drain -> no drop. statXP starts 0 so real gains are detected.",
    "                const xpPrev: number = this.statXP[stat];",
    "                if (xpPrev >= 0 && xp > xpPrev) {",
    "                    const gained: number = xp - xpPrev;",
    "                    this.xpDrops.push({ skill: stat, xp: gained, y: this.xpDropBottom });",
    "                    if (this.xpDrops.length > XP_MAX_DROPS) {",
    "                        this.xpDrops.shift();",
    "                    }",
    "                    const now: number = performance.now();",
    "                    // panel tracks the biggest gainer of the current burst, not the",
    "                    // last packet: a melee/ranged/magic hit drips small Hitpoints",
    "                    // (and Defence) xp alongside the real skill, and OSRS players",
    "                    // don't want the tracker hijacked every auto-retaliation",
    "                    if (now - this.xpLastGainAt > XP_BURST_MS) {",
    "                        this.xpRates.fill(0); // new burst",
    "                    }",
    "                    this.xpRates[stat] += gained;",
    "                    let best: number = this.xpLastSkill >= 0 ? this.xpLastSkill : stat;",
    "                    for (let s: number = 0; s < Skill.count; s++) {",
    "                        if (this.xpRates[s] > this.xpRates[best]) {",
    "                            best = s;",
    "                        }",
    "                    }",
    "                    this.xpLastSkill = best;",
    "                    this.xpLastGainAt = now;",
    "                }",
    "",
    "                this.statXP[stat] = xp;",
    "                this.statEffectiveLevel[stat] = level;"
   ],
   "note": "src/client/Client.ts @ old line 6685",
   "occ": 1,
   "pos": [
    6685,
    0,
    6683,
    6686
   ]
  }
 ]
}