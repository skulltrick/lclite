{
 "file": "webclient/src/client/Client.ts",
 "mod": "stat-orbs",
 "rev": "289",
 "generated_from": {
  "repo": "webclient",
  "head": "a52b321a9f42530f3dd2f1a02cd335c955791a12"
 },
 "hunks": [
  {
   "find": [
    "import IfType from '#/config/IfType.js';",
    "import { ComponentType, ButtonType } from '#/config/IfType.js';",
    "",
    "import ClientEntity from '#/dash3d/ClientEntity.js';"
   ],
   "replace": [
    "import IfType from '#/config/IfType.js';",
    "import { ComponentType, ButtonType } from '#/config/IfType.js';",
    "// lclite:stat-orbs",
    "import type { OrbBook, OrbBookCell, OrbLayout, OrbRun, OrbSpec } from '#/client/StatOrbs.js';",
    "import {",
    "    ORB_GLYPH_HP, ORB_GLYPH_PRAYER, ORB_GLYPH_RUN, ORB_ORIGIN_X, ORB_ORIGIN_Y, ORB_PANEL_H,",
    "    ORB_PANEL_W, ORB_RUN_CLIENTCODE, ORB_SPEC_EXTRA, ORB_STRIP_H, ORB_STRIP_W, ORB_STRIP_X,",
    "    statOrbsBook, statOrbsBookBox, statOrbsBookCell, statOrbsBookHit, statOrbsClampX,",
    "    statOrbsClampY, statOrbsDigitAdvance, statOrbsDigitGap, statOrbsGlassRadius, statOrbsGlyph,",
    "    statOrbsGlyphH, statOrbsGlyphOk, statOrbsGlyphW, statOrbsInBox, statOrbsInStrip,",
    "    statOrbsMarkPixel, statOrbsMarkSize, statOrbsNumberLeft,",
    "    statOrbsNumberTop, statOrbsNumberWidth, statOrbsOrbHit, statOrbsOrbLeft, statOrbsRun,",
    "    statOrbsScale, statOrbsSettings, statOrbsSpec, statOrbsSpecArmed, statOrbsSpecBox,",
    "    statOrbsSpecPercent, statOrbsSpecTextScale, statOrbsSpecWeapon",
    "} from '#/client/StatOrbs.js'; // custom (lclite \"stat-orbs\" mod): the settings parse, the readout font, the prayer-book / run-button / special-attack lookup, the quick-prayer panel's and the spec orb's geometry, and the placement clamps",
    "",
    "import ClientEntity from '#/dash3d/ClientEntity.js';"
   ],
   "note": "src/client/Client.ts @ old line 23",
   "occ": 1,
   "pos": [
    23,
    0,
    21,
    24
   ]
  },
  {
   "find": [
    "",
    "    minimapLoop(): void {",
    "        if (this.minimapState !== 0 || this.mouseClickButton !== 1 || !this.localPlayer) {",
    "            return;"
   ],
   "replace": [
    "",
    "    minimapLoop(): void {",
    "        // custom (lclite \"stat-orbs\" mod): the orb column and its prayer book sit ON the",
    "        // minimap panel, so a click that lands on one of them is not a walk. The orbs' own",
    "        // hook runs here, at the top of the engine's minimap click handling — and inside",
    "        // checkClickInput's guard, because minimapLoop is only reached through it — and",
    "        // consumes the click before the walk below can claim it. A click anywhere else on",
    "        // the panel is left to the engine, so walking the minimap is untouched.",
    "        // lclite:stat-orbs",
    "        if (this.orbsClickLoop()) {",
    "            return;",
    "        }",
    "",
    "        if (this.minimapState !== 0 || this.mouseClickButton !== 1 || !this.localPlayer) {",
    "            return;"
   ],
   "note": "src/client/Client.ts @ old line 2743",
   "occ": 1,
   "pos": [
    2743,
    0,
    2741,
    2744
   ]
  },
  {
   "find": [
    "    }",
    "",
    "    // todo: order",
    "    private gameDraw(): void {"
   ],
   "replace": [
    "    }",
    "",
    "    /// custom (lclite \"stat-orbs\" mod): fields + tunables live in this one lclite:stat-orbs",
    "    /// isolated slot (far from any other mod's hunks) so regen routes the whole",
    "    /// block to mods/stat-orbs; the three minimapDraw hooks stay separate hunks.",
    "    /// OSRS-style Hitpoints / Prayer / Run-energy data orbs on the minimap panel.",
    "    public orbsEnabled: boolean = false;",
    "    private orbsWereOn: boolean = false;",
    "    /// signature of the layout drawn last frame: a change (moved, resized, recoloured)",
    "    /// leaves stale orb pixels in the widget buffer, so the mapback is re-plotted once",
    "    /// to wipe them — see drawStatOrbs.",
    "    private orbsSig: string = '';",
    "    private orbsApiBound: boolean = false;",
    "    /// the sidebar's OWN stone strip left of the widget (canvas 516..549, the widget's",
    "    /// height): a second buffer this mod composites itself, because the widget buffer IS",
    "    /// the orbs' canvas and pixels outside it cannot exist. Its background is copied out",
    "    /// of the sidebar's own backvmid1 pixels every frame it is used, so it is",
    "    /// stone-identical to what is already on the canvas and can never hold a stale orb —",
    "    /// see orbsStripFrame. lclite:stat-orbs",
    "    private orbsStrip: PixMap | null = null;",
    "    private orbsStripUsed: boolean = false;",
    "",
    "    /// custom: the prayer book and the run/walk buttons, looked up in the LOADED",
    "    /// interface list (IfType.list is complete before login) — see orbsResolve(). Both",
    "    /// stay null when this revision's interfaces do not match, and the panel and the",
    "    /// run click are then simply not offered. lclite:stat-orbs",
    "    private orbsBook: OrbBook | null = null;",
    "    private orbsRun: OrbRun | null = null;",
    "    private orbsBookTried: boolean = false;",
    "    /// custom: the special-attack bar, found in the COMBAT tab's interface (see",
    "    /// orbsResolve): its energy varp and maximum, the LAYER the server hides for a weapon",
    "    /// without a special attack, the bar's own clickable component (the orb's click sends",
    "    /// an IF_BUTTON for it) and the varp its own label reads while the attack is armed.",
    "    /// Only that SHAPE is cached — the weapon's state and the armed flag are read live",
    "    /// every frame, because the weapon (and with it the whole combat interface) can change",
    "    /// on any tick. lclite:stat-orbs",
    "    private orbsSpec: OrbSpec | null = null;",
    "    private orbsSpecRoot: number = -1;",
    "    private orbsSpecApiBound: boolean = false;",
    "    /// custom: the spec orb's palette while the wielded weapon has NO special attack —",
    "    /// a DEAD orb, deliberately: a flat dark glass, a mid-grey fill and a dim grey",
    "    /// readout, so \"you cannot spend this\" is unmistakable at a glance. The first palette",
    "    /// was a light grey that read as a perfectly normal orb, which is half of why the orb",
    "    /// looked wrong on every weapon (the other half was the layer lookup — see",
    "    /// statOrbsSpec). lclite:stat-orbs",
    "    static readonly ORB_SPEC_GREY_BG: number = 0x35322e;",
    "    static readonly ORB_SPEC_GREY_FILL: number = 0x605d58;",
    "    static readonly ORB_SPEC_GREY_TEXT: number = 0x8a867f;",
    "    /// custom: the spec orb ARMED — the next attack will spend the energy. A lit ring on",
    "    /// the orb's own metal rim plus the bar's own @yel@ readout, which is exactly what the",
    "    /// combat tab does to its own label while it is armed. lclite:stat-orbs",
    "    static readonly ORB_SPEC_ARMED: number = 0xf2c14e;",
    "    static readonly ORB_SPEC_ARMED_TEXT: number = 0xffff00;",
    "    /// the quick-prayer panel's open state. Never persisted: a reload starts closed,",
    "    /// and switching the mod off closes it. orbsBookHover is the cell the cursor is on",
    "    /// (-1 for none) — the panel's only hover feedback. lclite:stat-orbs",
    "    private orbsPanelOpen: boolean = false;",
    "    private orbsBookHover: number = -1;",
    "",
    "    /// custom: the readout font's TABLE and its rasterizer live in the mod's pure core",
    "    /// (StatOrbs.ts: ORB_DIGITS / statOrbsGlyph), so mods/stat-orbs/tools/stat_orbs_test.ts",
    "    /// can rasterize a readout headlessly and compare it to these pixels. drawOrbNumber",
    "    /// plots the bitmaps it returns. lclite:stat-orbs",
    "    /// custom: the orb marks (the HP heart, the prayer star — the game's own 2004 Prayer",
    "    /// icon — and the run boot) live in the mod's pure core with the digit font, so",
    "    /// mods/stat-orbs/tools/stat_orbs_test.ts can check them: drawOrb refuses a mark that",
    "    /// is not a complete square of the master's size, and the run orb shipped for a while",
    "    /// with a 52-character bolt that the old length test silently dropped — an EMPTY orb,",
    "    /// with nothing anywhere to say why. lclite:stat-orbs",
    "    /// custom: the 9 anchor points of the panel's alt-drag placement, as a POSITIONAL",
    "    /// table. The drag layer stores the anchor's NAME ('MC') in localStorage, and a",
    "    /// runtime string must never be an object-literal KEY in bundled code: terser's",
    "    /// property mangler renames the keys (`{MC: …}` ships as `{av: …}`) so every",
    "    /// lookup with the stored name misses silently — which is exactly what kept canvas",
    "    /// surfaces from ever moving. Names and fractions are parallel arrays, matched by",
    "    /// index. lclite:stat-orbs",
    "    static readonly ORB_ANCH_NAMES: string[] = ['TL', 'TC', 'TR', 'ML', 'MC', 'MR', 'BL', 'BC', 'BR'];",
    "    static readonly ORB_ANCH: number[][] = [[0, 0], [0.5, 0], [1, 0], [0, 0.5], [0.5, 0.5], [1, 0.5], [0, 1], [0.5, 1], [1, 1]];",
    "",
    "    /// custom: the orb column's live geometry. Every key is read HERE, every frame, at",
    "    /// this mod's own hook (rule 5) — including the lcmStatOrbs* placement keys, which",
    "    /// only the panel's drag layer writes (alt+drag; the panel hit-tests the box this",
    "    /// returns through window.lcmStatOrbsBounds). lclite:stat-orbs",
    "    private orbLayout(): OrbLayout {",
    "        const S = statOrbsSettings((key: string) => localStorage.getItem(key));",
    "        const size: number = Math.max(20, Math.min(28, parseInt(localStorage.getItem('statOrbsSize') || '') || 22));",
    "        const r: number = (size / 2) | 0;",
    "        const numbers: string = localStorage.getItem('statOrbsNumbers') || 'left';",
    "        // 'left' readouts reserve the strip for three digits at the chosen SCALE (14px at",
    "        // 1x, the shipped default — see statOrbsOrbLeft); the other two modes have no text",
    "        // beside the orbs, so they hug the frame edge. Bigger digits pay for themselves by",
    "        // pushing the whole column further right, over the map's own left edge.",
    "        const orbLeft: number = statOrbsOrbLeft(numbers, S.numberScale);",
    "        // even spread down the strip: the compass owns y < 33 and the widget ends at 156,",
    "        // so the three orbs share y 34..154 whatever their size (step = 60 - r).",
    "        const step: number = 60 - r;",
    "        const boxW: number = orbLeft + 2 * r;",
    "        const boxH: number = 2 * r + 2 * step;",
    "        let ox: number = 0;",
    "        let oy: number = 34;",
    "        const ai: number = Client.ORB_ANCH_NAMES.indexOf(localStorage.getItem('lcmStatOrbsAnchor') || '');",
    "        if (ai >= 0) {",
    "            const k: number[] = Client.ORB_ANCH[ai];",
    "            const off: string[] = (localStorage.getItem('lcmStatOrbsOffset') || '0,0').split(',');",
    "            ox = Math.round(172 * k[0]) + (parseInt(off[0]) || 0);",
    "            oy = Math.round(156 * k[1]) + (parseInt(off[1]) || 0);",
    "        }",
    "        // clamped FLUSH (no margin): the widget itself is the boundary, so a canvas",
    "        // surface can never be lost off-screen — the panel clamps the same way. The LEFT",
    "        // edge is the exception: the mod paints the sidebar's own stone strip left of the",
    "        // widget itself, so a column may hang ORB_STRIP_W past it. statOrbsClampX is the",
    "        // one place that number lives, and the drag layer is told the same one (the 8th",
    "        // element of registerCanvas), so the ghost and the drawn orbs agree. lclite:stat-orbs",
    "        ox = statOrbsClampX(ox, boxW);",
    "        oy = statOrbsClampY(oy, boxH);",
    "",
    "        // The spec orb is its OWN canvas surface, with its own keys and its own reset,",
    "        // because it deliberately sits away from the column: the panel's bottom left,",
    "        // clear of the three orbs and of the wiki button that owns the bottom right.",
    "        // Until it is dragged it FOLLOWS the column — the same 4px gap and the same",
    "        // bottom edge at every size and number scale — so moving or resizing the column",
    "        // can never land it on top of one of the three. lclite:stat-orbs",
    "        const specR: number = r + ORB_SPEC_EXTRA;",
    "        let specX: number = 0;",
    "        let specY: number = 0;",
    "        const si: number = Client.ORB_ANCH_NAMES.indexOf(localStorage.getItem('lcmStatOrbsSpecAnchor') || '');",
    "        if (si >= 0) {",
    "            const k: number[] = Client.ORB_ANCH[si];",
    "            const soff: string[] = (localStorage.getItem('lcmStatOrbsSpecOffset') || '0,0').split(',');",
    "            specX = Math.round(172 * k[0]) + (parseInt(soff[0]) || 0);",
    "            specY = Math.round(156 * k[1]) + (parseInt(soff[1]) || 0);",
    "            // clamped FLUSH, exactly like the column and like the panel's own drag layer",
    "            // — the stone strip overhang on the left included",
    "            specX = statOrbsClampX(specX, 2 * specR);",
    "            specY = statOrbsClampY(specY, 2 * specR);",
    "        } else {",
    "            const specBox: number[] = statOrbsSpecBox(ox + orbLeft + 2 * r, oy + boxH, r);",
    "            specX = specBox[0];",
    "            specY = specBox[1];",
    "        }",
    "",
    "        return { r: r, orbLeft: orbLeft, step: step, ox: ox, oy: oy, boxW: boxW, boxH: boxH, numbers: numbers, scale: S.numberScale, pie: localStorage.getItem('statOrbsFill') === 'pie', pulse: localStorage.getItem('statOrbsPulse') !== 'false', specX: specX, specY: specY, specR: specR };",
    "    }",
    "",
    "    /// custom (lclite \"stat-orbs\" mod): the three data orbs, painted into the areaMap",
    "    /// widget buffer (172x156, composited at canvas 550,4) at the END of minimapDraw()",
    "    /// — the same ride as the minimap itself, so they land ON TOP of the interface and",
    "    /// the gpu mod's overlay upload picks them up like every other HUD pixel. Every",
    "    /// pixel written is opaque: the stone strip left of the map window is painted once",
    "    /// at boot and never re-cleared, so an alpha blend there would accumulate and",
    "    /// darken it a little more every tick.",
    "    private drawStatOrbs(): void {",
    "        const L = this.orbLayout();",
    "        const hpCur: number = this.statEffectiveLevel[3];",
    "        const hpMax: number = this.statBaseLevel[3];",
    "        const prCur: number = this.statEffectiveLevel[5];",
    "        const prMax: number = this.statBaseLevel[5];",
    "        const hpFrac: number = hpMax > 0 ? hpCur / hpMax : 0;",
    "        const prFrac: number = prMax > 0 ? prCur / prMax : 0;",
    "        // runenergy arrives as energy/100 on the wire (0-100)",
    "        const rnFrac: number = Math.max(0, Math.min(1, this.runenergy / 100));",
    "        const hpRgb: number = Client.orbColour('statOrbsHpColor', 0xe82623);",
    "        const prRgb: number = Client.orbColour('statOrbsPrayerColor', 0xd9a318);",
    "        const rnRgb: number = Client.orbColour('statOrbsRunColor', 0x71c8e8);",
    "        // the special attack orb: a RED background with a GREEN depleting fill, which is",
    "        // the 2004 spec bar's own colour scheme — and a plain grey orb for a weapon with",
    "        // no special attack, which still reads the energy out. lclite:stat-orbs",
    "        const spRgb: number = Client.orbColour('statOrbsSpecColor', 0x8b1a1a);",
    "        const spFill: number = Client.orbColour('statOrbsSpecFill', 0x3cbf2e);",
    "",
    "        // stale-pixel wipe, only when what was drawn actually changed: moving, resizing",
    "        // or recolouring the orbs abandons pixels on stone that is never repainted — and",
    "        // so does a changed READOUT, whose glyphs change width with the value (100 -> 45",
    "        // leaves a stale third digit behind) and whose colour follows the HP fraction.",
    "        // The mapback's map window is a transparent hole, so re-plotting it restores the",
    "        // stone and leaves the map alone (minimapDraw re-blits the map this same tick).",
    "        //",
    "        // The quick-prayer panel adds state that moves no number at all: a prayer",
    "        // toggling, the prayer level unlocking an icon, the panel opening or closing, the",
    "        // cursor crossing onto another cell. All of it is in the signature, or the panel",
    "        // would show yesterday's prayers. lclite:stat-orbs",
    "        this.orbsResolve();",
    "        const runOn: boolean = this.orbsRun !== null && this.var[this.orbsRun.varp] === 1;",
    "        // LIVE every frame: the bar layer's hide flag is the server's own answer to \"does",
    "        // this weapon have a special attack\" (it hides the bar for one that does not), and",
    "        // the varp is the energy itself — the same value the game's own bar draws.",
    "        // lclite:stat-orbs",
    "        const spec: OrbSpec | null = this.orbsSpec;",
    "        const specHas: boolean = statOrbsSpecWeapon(IfType.list, spec);",
    "        const specPct: number = spec === null ? 0 : statOrbsSpecPercent(this.var[spec.varp], spec.max);",
    "        // ARMED is the bar's own flag — its active text reads `sa_attack` with `gt,0` —",
    "        // and it is read live every frame for the same reason the weapon flag is: the",
    "        // weapon can change on any tick, and equipping a different one clears it.",
    "        // lclite:stat-orbs",
    "        const specArmed: boolean = spec !== null && specHas && statOrbsSpecArmed(this.var[spec.armed], spec);",
    "        const bookBox: number[] = statOrbsBookBox(L.ox + L.orbLeft + 2 * L.r);",
    "        // the cursor's cell, straight from the live canvas position, so the hover box is",
    "        // already right for the frame being drawn. lclite:stat-orbs",
    "        this.orbsBookHover = this.orbsPanelOpen && this.orbsBook !== null ? statOrbsBookHit(this.mouseX - ORB_ORIGIN_X, this.mouseY - ORB_ORIGIN_Y, bookBox) : -1;",
    "        let bookSig: string = '0';",
    "        if (this.orbsBook !== null) {",
    "            let active: number = 0;",
    "            for (let i: number = 0; i < this.orbsBook.cells.length; i++) {",
    "                if (this.var[this.orbsBook.cells[i].varp] === 1) {",
    "                    active |= 1 << i;",
    "                }",
    "            }",
    "            bookSig = (this.orbsPanelOpen ? '1' : '0') + ',' + active + ',' + this.statBaseLevel[5] + ',' + bookBox[0] + ',' + this.orbsBookHover;",
    "        }",
    "        // the spec orb's own state rides the same signature: its percentage, its weapon",
    "        // state (which recolours it), its spot and its size all leave stale pixels behind",
    "        // when they change. 'x' = no spec bar in this revision's interfaces, i.e. nothing",
    "        // drawn — which must wipe too, if the orb was drawn a frame ago. lclite:stat-orbs",
    "        const specSig: string = spec === null ? 'x' : (specHas ? '1' : '0') + ',' + (specArmed ? '1' : '0') + ',' + specPct + ',' + L.specX + ',' + L.specY + ',' + L.specR + ',' + spRgb + ',' + spFill;",
    "        const sig: string = L.ox + ',' + L.oy + ',' + L.r + ',' + L.numbers + ',' + L.scale + ',' + (L.pie ? 'p' : 'l') + ',' + hpRgb + ',' + prRgb + ',' + rnRgb + ',' + hpCur + ',' + prCur + ',' + this.runenergy + ',' + (runOn ? 1 : 0) + ',' + specSig + ',' + bookSig;",
    "        if (sig !== this.orbsSig) {",
    "            if (this.orbsSig !== '') {",
    "                this.mapback?.plotSprite(0, 0);",
    "            }",
    "            this.orbsSig = sig;",
    "        }",
    "",
    "        const cx: number = L.ox + L.orbLeft + L.r;",
    "        const cy0: number = L.oy + L.r;",
    "",
    "        // The prayer book goes UNDER the orbs. At 3x the readouts reach across the",
    "        // panel's own left edge, and a prayer book you cannot see your prayer points",
    "        // through is worse than one icon behind an orb. lclite:stat-orbs",
    "        if (this.orbsPanelOpen && this.orbsBook !== null) {",
    "            this.drawOrbBook(bookBox);",
    "        }",
    "",
    "        // OSRS order, top to bottom: Hitpoints, Prayer, Run energy. RUN ON lightens the",
    "        // run orb's glass so the toggle's own state is readable at a glance — the same job",
    "        // the options tab's run button does with its active graphic, and the only feedback",
    "        // a one-click toggle can give. It is baked into the colour HERE because the two",
    "        // drawing passes below both draw from this list. lclite:stat-orbs",
    "        const orbs: { cy: number; rgb: number; frac: number; value: number; textRgb: number; glyph: string }[] = [",
    "            { cy: cy0, rgb: hpRgb, frac: hpFrac, value: hpCur, textRgb: hpFrac > 0.66 ? Colour.GREEN : hpFrac > 0.33 ? Colour.YELLOW : Colour.RED, glyph: ORB_GLYPH_HP },",
    "            { cy: cy0 + L.step, rgb: prRgb, frac: prFrac, value: prCur, textRgb: Colour.WHITE, glyph: ORB_GLYPH_PRAYER },",
    "            { cy: cy0 + 2 * L.step, rgb: runOn ? Client.orbShade(rnRgb, 1.35) : rnRgb, frac: rnFrac, value: this.runenergy, textRgb: Colour.WHITE, glyph: ORB_GLYPH_RUN }",
    "        ];",
    "",
    "        // the orbs themselves, into the widget buffer (dx 0) — and then the very same",
    "        // orbs into the sidebar's stone strip on the widget's left, where a dragged",
    "        // column may hang. Two passes over one drawing routine: everything outside the",
    "        // buffer being painted into is clipped by Pix2D, so an overhanging column is",
    "        // drawn half in each. lclite:stat-orbs",
    "        this.orbsDrawOrbs(L, 0, orbs, spRgb, spFill, specHas, specArmed, specPct);",
    "        this.orbsStripFrame(L, orbs, spRgb, spFill, specHas, specArmed, specPct);",
    "",
    "        // placement bridge for the panel's drag layer (positional arrays only — object",
    "        // literals arrive key-mangled across the bundle boundary). Bound once; the box",
    "        // stays live, and reports null while the mod is off so alt+drag cannot grab it.",
    "        const W: any = window as any;",
    "        if (!this.orbsApiBound && W['lcmAnchor']) {",
    "            this.orbsApiBound = true;",
    "            W['lcmStatOrbsBounds'] = (): number[] | null => {",
    "                const b = this.orbLayout();",
    "                return this.orbsEnabled ? [b.ox, b.oy, b.boxW, b.boxH] : null;",
    "            };",
    "            W['lcmAnchor']['registerCanvas']?.([",
    "                'stat-orbs', 'lcmStatOrbsAnchor', 'lcmStatOrbsOffset', 'TL', '0,34',",
    "                () => (W['lcmStatOrbsBounds'] ? W['lcmStatOrbsBounds']() : null),",
    "                // the region, then the OVERHANG: distances OUTWARD, in buffer px,",
    "                // so ORB_STRIP_W on the left lets the drag layer follow the orbs into",
    "                // the sidebar's stone strip this mod paints itself. lclite:stat-orbs",
    "                [550, 4, 172, 156, 172, 156], [ORB_STRIP_W, 0, 0, 0]",
    "            ]);",
    "            // the spec orb is a SECOND canvas surface on the same widget buffer: its own",
    "            // keys, so Alt+drag and its own reset row move it alone. Its bounds are a",
    "            // closure, so the box it publishes is always the one being drawn.",
    "            // lclite:stat-orbs",
    "            W['lcmAnchor']['registerCanvas']?.(['stat-orbs-spec', 'lcmStatOrbsSpecAnchor', 'lcmStatOrbsSpecOffset', 'BL', '40,128', () => {",
    "                const b = this.orbLayout();",
    "                return this.orbsEnabled && this.orbsSpec !== null ? [b.specX, b.specY, 2 * b.specR, 2 * b.specR] : null;",
    "            }, [550, 4, 172, 156, 172, 156], [ORB_STRIP_W, 0, 0, 0]]);",
    "        }",
    "    }",
    "",
    "    /// custom: the orbs and their readouts, drawn at an x offset — dx 0 paints into the",
    "    /// minimap widget buffer (the orbs' own canvas), dx ORB_STRIP_W paints the SAME orbs",
    "    /// into the sidebar's stone strip on the widget's left. Every pixel outside the bound",
    "    /// buffer is skipped (drawOrb bounds-checks, orbPixel bounds-checks), so each pass",
    "    /// inks only its own half of an overhanging column and the two together are exactly",
    "    /// what one pass over a wider buffer would have drawn. lclite:stat-orbs",
    "    private orbsDrawOrbs(L: OrbLayout, dx: number, orbs: { cy: number; rgb: number; frac: number; value: number; textRgb: number; glyph: string }[], spRgb: number, spFill: number, specHas: boolean, specArmed: boolean, specPct: number): void {",
    "        const cx: number = L.ox + L.orbLeft + L.r + dx;",
    "        for (let i: number = 0; i < orbs.length; i++) {",
    "            const orb = orbs[i];",
    "            // OSRS low-hitpoints warning: flash the orb while health is nearly gone",
    "            let rgb: number = L.pulse && orb.frac > 0 && orb.frac <= 0.25 && Client.loopCycle % 16 < 8 ? Client.orbShade(orb.rgb, 1.7) : orb.rgb;",
    "            this.drawOrb(cx, orb.cy, L.r, rgb, orb.frac, L.pie, orb.glyph, 0);",
    "",
    "            const text: string = String(orb.value);",
    "            if (L.numbers === 'left') {",
    "                this.drawOrbNumber(text, statOrbsNumberLeft(cx, L.r, text, L.scale), orb.cy, orb.textRgb, L.scale);",
    "            } else if (L.numbers === 'inside') {",
    "                this.drawOrbNumber(text, cx - (statOrbsNumberWidth(text, L.scale) >> 1), orb.cy, orb.textRgb, L.scale);",
    "            }",
    "        }",
    "",
    "        // the special attack orb: a bit bigger than the others, at the panel's bottom",
    "        // left. The percentage IS its information, so it is always drawn INSIDE it, at the",
    "        // largest scale that still fits its glass — the 'Orb numbers' setting governs the",
    "        // column's readouts, and a number beside this orb would land on the run orb above",
    "        // it. lclite:stat-orbs",
    "        if (this.orbsSpec !== null) {",
    "            const scx: number = L.specX + L.specR + dx;",
    "            const scy: number = L.specY + L.specR;",
    "            this.drawOrb(scx, scy, L.specR, specHas ? spFill : Client.ORB_SPEC_GREY_FILL, specPct / 100, L.pie, '', specHas ? spRgb : Client.ORB_SPEC_GREY_BG);",
    "            // ARMED: a lit ring on the orb's own metal rim — the one place a state can",
    "            // live without moving a pixel of the orb — and the readout in the bar's own",
    "            // @yel@, so the orb and the combat tab say the same thing. A weapon with no",
    "            // special attack gets neither: it is a dead grey orb, number and all.",
    "            // lclite:stat-orbs",
    "            if (specArmed) {",
    "                Client.orbRing(scx, scy, statOrbsGlassRadius(L.specR) + 1, Client.ORB_SPEC_ARMED);",
    "            }",
    "            const specText: string = String(specPct);",
    "            const specScale: number = statOrbsSpecTextScale(L.scale, L.specR - 3);",
    "            const specInk: number = !specHas ? Client.ORB_SPEC_GREY_TEXT : specArmed ? Client.ORB_SPEC_ARMED_TEXT : Colour.WHITE;",
    "            this.drawOrbNumber(specText, scx - (statOrbsNumberWidth(specText, specScale) >> 1), scy, specInk, specScale);",
    "        }",
    "    }",
    "",
    "    /// custom: the OVERHANG pass — the sidebar's own stone strip left of the minimap",
    "    /// widget, which is where a dragged orb column may hang. The orbs live in the widget",
    "    /// buffer (172x156) and pixels left of it cannot exist there, so the strip is a",
    "    /// SECOND buffer this mod composites itself at (516, 4): its background is copied",
    "    /// straight out of the sidebar's own backvmid1 pixels (the client paints that sprite",
    "    /// at 516,4, so the strip IS its left 34 columns — pixel for pixel the stone already",
    "    /// on the canvas), and the same orbs are drawn over it shifted right by the strip's",
    "    /// width. Rebuilt from the stone on every frame it is used, so it can never keep a",
    "    /// stale orb: the moment nothing hangs left, the bare stone is composited once more",
    "    /// and the strip goes quiet until it is needed again. lclite:stat-orbs",
    "    private orbsStripFrame(L: OrbLayout, orbs: { cy: number; rgb: number; frac: number; value: number; textRgb: number; glyph: string }[], spRgb: number, spFill: number, specHas: boolean, specArmed: boolean, specPct: number): void {",
    "        const live: boolean = statOrbsInStrip(L.ox, L.specX);",
    "        if (!live && !this.orbsStripUsed) {",
    "            return;                                     // nothing hangs left, nothing left behind",
    "        }",
    "",
    "        const strip: PixMap | null = this.orbsStripStone();",
    "        if (strip === null) {",
    "            return;",
    "        }",
    "",
    "        if (live) {",
    "            this.orbsDrawOrbs(L, ORB_STRIP_W, orbs, spRgb, spFill, specHas, specArmed, specPct);",
    "        }",
    "        strip.draw(ORB_STRIP_X, ORB_ORIGIN_Y);",
    "        this.orbsStripUsed = live;",
    "        this.areaMap?.setPixels();                      // the caller's buffer, as minimapDraw left it",
    "    }",
    "",
    "    /// custom: drop the strip back to bare stone — the mod was switched off with the orbs",
    "    /// hanging in it, and nothing else ever repaints that strip, so they would simply stay",
    "    /// there. One composite, then the strip goes quiet. lclite:stat-orbs",
    "    private orbsStripClear(): void {",
    "        if (!this.orbsStripUsed) {",
    "            return;",
    "        }",
    "",
    "        const strip: PixMap | null = this.orbsStripStone();",
    "        if (strip === null) {",
    "            return;",
    "        }",
    "",
    "        strip.draw(ORB_STRIP_X, ORB_ORIGIN_Y);",
    "        this.orbsStripUsed = false;",
    "        this.areaMap?.setPixels();",
    "    }",
    "",
    "    /// custom: the strip buffer, filled with the sidebar's own stone and bound to Pix2D,",
    "    /// ready for the orbs to be drawn into it. Null while the sidebar's background buffer",
    "    /// does not exist yet (before prepareGame). lclite:stat-orbs",
    "    private orbsStripStone(): PixMap | null {",
    "        const stone: PixMap | null = this.areaBackvmid1;",
    "        if (stone === null) {",
    "            return null;",
    "        }",
    "",
    "        let strip: PixMap | null = this.orbsStrip;",
    "        if (strip === null) {",
    "            strip = new PixMap(ORB_STRIP_W, ORB_STRIP_H);",
    "            this.orbsStrip = strip;",
    "        }",
    "",
    "        stone.setPixels();                              // the sidebar's own background buffer",
    "        const src: Int32Array = Pix2D.pixels;",
    "        const stride: number = Pix2D.width;",
    "        const rows: number = Math.min(ORB_STRIP_H, (src.length / stride) | 0);",
    "        const dst: Int32Array = strip.data;",
    "        for (let y: number = 0; y < rows; y++) {",
    "            const from: number = y * stride;",
    "            const to: number = y * ORB_STRIP_W;",
    "            for (let x: number = 0; x < ORB_STRIP_W; x++) {",
    "                dst[to + x] = src[from + x];",
    "            }",
    "        }",
    "",
    "        strip.setPixels();",
    "        return strip;",
    "    }",
    "",
    "    /// custom: one procedural orb. Hard 1px black outline, a bevelled stone rim in the",
    "    /// mapback's own palette (lit upper-left, shadowed lower-right), a dark glass body,",
    "    /// and a liquid (or clockwise pie) fill shaded in three HARD bands. No anti-aliasing",
    "    /// and no smooth gradient anywhere: the 2004/5 interface is built from discrete",
    "    /// pixel steps, and a gradient is exactly what made the old orbs read as modern",
    "    /// vector art. The stat glyph sits in the middle with a 1px outline and the gloss",
    "    /// spark on the upper-left shoulder, which is where OSRS puts it too.",
    "    private drawOrb(cx: number, cy: number, r: number, rgb: number, frac: number, pie: boolean, glyph: string, bg: number): void {",
    "        if (frac < 0) {",
    "            frac = 0;",
    "        } else if (frac > 1) {",
    "            frac = 1;",
    "        }",
    "",
    "        // the glass's radius comes from the core (statOrbsGlassRadius), so the mark",
    "        // clipping and the spec orb's armed ring land on the very pixels the rim below",
    "        // draws — three places that must agree about where the glass ends. lclite:stat-orbs",
    "        const innerR: number = statOrbsGlassRadius(r);",
    "        const tau: number = 6.283185307179586;",
    "        const sweep: number = tau * frac;",
    "        const surface: number = cy + innerR - 2 * innerR * frac;  // liquid level (liquid fill)",
    "        const dst: Int32Array = Pix2D.pixels;",
    "        const w: number = Pix2D.width;",
    "        const rr: number = r * r;",
    "        const ir: number = innerR * innerR;",
    "        const rimIn: number = (r - 1) * (r - 1);",
    "",
    "        for (let dy: number = -r; dy <= r; dy++) {",
    "            const y: number = cy + dy;",
    "            if (y < 0 || y >= Pix2D.height) {",
    "                continue;",
    "            }",
    "",
    "            for (let dx: number = -r; dx <= r; dx++) {",
    "                const x: number = cx + dx;",
    "                if (x < 0 || x >= w) {",
    "                    continue;",
    "                }",
    "",
    "                const d2: number = dx * dx + dy * dy;",
    "                if (d2 > rr) {",
    "                    continue;",
    "                }",
    "",
    "                let colour: number;",
    "                if (d2 > rimIn) {",
    "                    colour = 0x0a0a0a;                            // hard outer outline",
    "                } else if (d2 > ir) {",
    "                    // bevelled metal rim: lit along the TOP half, shadowed below. A",
    "                    // diagonal split read as one flat dark ring against the stone frame,",
    "                    // and the old colours were too close to the stone to see at all.",
    "                    colour = dy < 0 ? 0x7d7466 : 0x2a251f;",
    "                } else {",
    "                    let filled: boolean;",
    "                    if (pie) {",
    "                        // angle clockwise from 12 o'clock (centre pixel: pin it filled)",
    "                        let ang: number = d2 === 0 ? 0 : Math.atan2(dx, -dy);",
    "                        if (ang < 0) {",
    "                            ang += tau;",
    "                        }",
    "                        filled = ang < sweep;",
    "                    } else {",
    "                        filled = y >= surface;",
    "                    }",
    "",
    "                    const d: number = Math.sqrt(d2);",
    "                    if (filled) {",
    "                        const t: number = d / innerR;",
    "                        colour = t > 0.84 ? Client.orbShade(rgb, 0.62) : t > 0.55 ? Client.orbShade(rgb, 0.82) : rgb;",
    "                        if (!pie && y - surface < 1) {",
    "                            colour = Client.orbShade(rgb, 1.45);   // meniscus line",
    "                        }",
    "                    } else if (bg === 0) {",
    "                        colour = d > innerR - 1.5 ? 0x2b2721 : 0x161310;   // the dark glass every stat orb has",
    "                    } else {",
    "                        // the spec orb's own background — red for a weapon with a special",
    "                        // attack, grey for one without. Two HARD bands, like every other",
    "                        // orb: never a gradient.",
    "                        colour = d > innerR - 1.5 ? Client.orbShade(bg, 1.3) : Client.orbShade(bg, 0.72);",
    "                    }",
    "                }",
    "",
    "                dst[x + y * w] = colour;",
    "            }",
    "        }",
    "",
    "        // The stat mark: two passes so the outline never eats the ink. It is drawn at the",
    "        // largest odd size the glass can carry (statOrbsMarkSize — 15px at the shipped orb",
    "        // size, 2.1x the 7px mark this mod shipped first) and CLIPPED TO THE GLASS, because",
    "        // the glass is a circle: a square mark's corners would otherwise be plotted over",
    "        // the orb's own metal rim. The outline is what keeps a mark legible over both the",
    "        // liquid and the dark glass. lclite:stat-orbs",
    "        const markSize: number = statOrbsMarkSize(innerR);",
    "        if (markSize > 0 && statOrbsGlyphOk(glyph)) {",
    "            const ink: number = Client.orbShade(rgb, 1.8);",
    "            const gx: number = cx - (markSize >> 1);",
    "            const gy: number = cy - (markSize >> 1);",
    "            for (let pass: number = 0; pass < 2; pass++) {",
    "                const col: number = pass === 0 ? 0x1c0f0c : ink;",
    "                for (let j: number = 0; j < markSize; j++) {",
    "                    for (let i: number = 0; i < markSize; i++) {",
    "                        if (!statOrbsMarkPixel(glyph, markSize, i, j)) {",
    "                            continue;",
    "                        }",
    "                        if (pass === 0) {",
    "                            Client.orbPixel(gx + i - 1, gy + j, col, ir, cx, cy);",
    "                            Client.orbPixel(gx + i + 1, gy + j, col, ir, cx, cy);",
    "                            Client.orbPixel(gx + i, gy + j - 1, col, ir, cx, cy);",
    "                            Client.orbPixel(gx + i, gy + j + 1, col, ir, cx, cy);",
    "                        } else {",
    "                            Client.orbPixel(gx + i, gy + j, col, ir, cx, cy);",
    "                        }",
    "                    }",
    "                }",
    "            }",
    "        }",
    "",
    "        // 2004 gloss: a small hard-edged glint on the upper-left shoulder — a rounded",
    "        // blob (corners cut), which reads as wet glass; a plus-shaped cluster reads as a",
    "        // UI sparkle sitting on top of the orb instead.",
    "        const hx: number = cx - innerR + 1;",
    "        const hy: number = cy - innerR + 1;",
    "        Client.orbPixel(hx + 1, hy, 0xf7f3e8);",
    "        Client.orbPixel(hx + 2, hy, 0xf7f3e8);",
    "        Client.orbPixel(hx, hy + 1, 0xf7f3e8);",
    "        Client.orbPixel(hx + 1, hy + 1, 0xffffff);",
    "        Client.orbPixel(hx + 2, hy + 1, 0xf7f3e8);",
    "        Client.orbPixel(hx + 1, hy + 2, 0xf7f3e8);",
    "    }",
    "",
    "    /// custom: a readout in the tiny orb font — 4px advance, 5px tall, 1px black shadow",
    "    /// at scale 1 (the 2004 way of putting text on the stone frame), with every one of",
    "    /// those numbers multiplied by the SCALE setting: each glyph pixel becomes an s x s",
    "    /// block and the shadow moves a whole block, so the digits stay square and stay on",
    "    /// the pixel grid at 2x and 3x instead of smearing. x is the LEFT edge of the block;",
    "    /// the digits are drawn vertically centred on cy. lclite:stat-orbs",
    "    private drawOrbNumber(text: string, x: number, cy: number, rgb: number, scale: number): void {",
    "        const s: number = statOrbsScale(scale);",
    "        const gw: number = statOrbsGlyphW(s);",
    "        const gh: number = statOrbsGlyphH(s);",
    "        const adv: number = statOrbsDigitAdvance(s);",
    "        const off: number = statOrbsDigitGap(s);       // the shadow drops one whole block",
    "        const y: number = statOrbsNumberTop(cy, s);",
    "        for (let pass: number = 0; pass < 2; pass++) {",
    "            const dx0: number = pass === 0 ? off : 0;",
    "            const dy0: number = pass === 0 ? off : 0;",
    "            const col: number = pass === 0 ? 0x000000 : rgb;",
    "            for (let i: number = 0; i < text.length; i++) {",
    "                const d: number = text.charCodeAt(i) - 48;",
    "                if (d < 0 || d > 9) {",
    "                    continue;",
    "                }",
    "                const ink: Uint8Array = statOrbsGlyph(d, s);",
    "                for (let py: number = 0; py < gh; py++) {",
    "                    for (let px: number = 0; px < gw; px++) {",
    "                        if (ink[py * gw + px] !== 1) {",
    "                            continue;",
    "                        }",
    "                        Client.orbPixel(x + i * adv + px + dx0, y + py + dy0, col);",
    "                    }",
    "                }",
    "            }",
    "        }",
    "    }",
    "",
    "    /// custom: resolve the prayer book and the run/walk buttons out of the LOADED",
    "    /// interface list. IfType.list is filled from the interface jag before login, so this",
    "    /// normally answers on the first frame; while the list is still empty it simply",
    "    /// leaves both null and tries again next frame. Both lookups are STRUCTURAL (see",
    "    /// StatOrbs.ts) — no component id is hardcoded anywhere, because ids are assigned by",
    "    /// the interface packer and are a revision-specific accident. lclite:stat-orbs",
    "    private orbsResolve(): void {",
    "        if (IfType.list.length === 0) {",
    "            return;",
    "        }",
    "",
    "        if (this.orbsBook === null || this.orbsRun === null) {",
    "            this.orbsBookTried = true;",
    "            this.orbsBook = statOrbsBook(IfType.list);",
    "",
    "            // the run varp is the one the cache marks clientcode 7 — the very test the server",
    "            // makes to find it (VarPlayerType.RUN), so this cannot drift from the game's own",
    "            // idea of which varp run energy lives in. lclite:stat-orbs",
    "            let runVarp: number = -1;",
    "            for (let i: number = 0; i < VarpType.list.length; i++) {",
    "                if (VarpType.list[i] && VarpType.list[i].clientcode === ORB_RUN_CLIENTCODE) {",
    "                    runVarp = i;",
    "                    break;",
    "                }",
    "            }",
    "            this.orbsRun = statOrbsRun(IfType.list, runVarp);",
    "        }",
    "",
    "        // The special-attack bar is looked up in the COMBAT tab's own interface — tab 0,",
    "        // the one the server swaps per weapon category (`if_settab($interface, 0)`) — and",
    "        // re-resolved whenever THAT changes, because every category has its own bar layer",
    "        // and only the one on screen has a live hide flag. Nothing else is cached here:",
    "        // the weapon, and so the bar's hidden state, can change on any tick.",
    "        // lclite:stat-orbs",
    "        const root: number = this.sideIcon[0];",
    "        if (root !== this.orbsSpecRoot) {",
    "            this.orbsSpecRoot = root;",
    "            this.orbsSpec = statOrbsSpec(IfType.list, root);",
    "        }",
    "    }",
    "",
    "    /// custom: the orb column's own click hook — the run orb's toggle, the prayer orb's",
    "    /// book, and the book's fifteen cells. It runs at the TOP of the engine's own minimap",
    "    /// click handling (minimapLoop, which the engine only reaches through mainloop's",
    "    /// checkClickInput guard) and CONSUMES the clicks it takes, so a click on an orb or on",
    "    /// the book can never also walk the minimap. Returns true when it took the click.",
    "    ///",
    "    /// It hangs off minimapLoop rather than the checkClickInput block on purpose: the wiki",
    "    /// button's hook already owns the only free insertion island there, and a second",
    "    /// insertion next to it would make the two mods one patch island — wiki-lookup's",
    "    /// corpus would then carry this mod's lines, and stripping wiki-lookup would take the",
    "    /// orbs' clicks with it. lclite:stat-orbs",
    "    private orbsClickLoop(): boolean {",
    "        if (!this.localPlayer || this.mouseClickButton !== 1) {",
    "            return false;",
    "        }",
    "",
    "        this.orbsEnabled = localStorage.getItem('statOrbs') === 'true';",
    "        if (!this.orbsEnabled) {",
    "            this.orbsPanelOpen = false;",
    "            this.orbsBookHover = -1;",
    "            return false;",
    "        }",
    "",
    "        this.orbsResolve();",
    "        const S = statOrbsSettings((key: string) => localStorage.getItem(key));",
    "        const L = this.orbLayout();",
    "        const mx: number = this.mouseClickX - ORB_ORIGIN_X;",
    "        const my: number = this.mouseClickY - ORB_ORIGIN_Y;",
    "        const cx: number = L.ox + L.orbLeft + L.r;",
    "        const spec: OrbSpec | null = this.orbsSpec;",
    "",
    "        // the orbs themselves, in OSRS order: 0 Hitpoints (a readout — it does nothing,",
    "        // but it is still ours to swallow), 1 Prayer (the book), 2 Run energy (the toggle).",
    "        // All three consume: the orbs straddle the map window's own left edge, so half of",
    "        // each one would otherwise set a walk flag under the cursor.",
    "        for (let i: number = 0; i < 3; i++) {",
    "            if (!statOrbsOrbHit(mx, my, cx, L.oy + L.r + i * L.step, L.r)) {",
    "                continue;",
    "            }",
    "",
    "            if (i === 1 && S.prayerPanel && this.orbsBook !== null) {",
    "                this.orbsPanelOpen = !this.orbsPanelOpen;",
    "                this.mouseClickButton = 0;",
    "            } else if (i === 2 && S.runClick && this.orbsRun !== null) {",
    "                this.orbsRunToggle();",
    "                this.mouseClickButton = 0;",
    "            }",
    "",
    "            return true;                // an orb owns every click that lands on it",
    "        }",
    "",
    "        // The spec orb straddles the map window's rim exactly like the column does, so",
    "        // it always swallows its own click rather than setting a walk flag under the",
    "        // cursor. On top of that it is the ONE orb whose click does something: it sends",
    "        // the combat tab's own spec-bar click, which arms (or disarms) the next attack.",
    "        // A weapon with no special attack has nothing to arm, so a dead grey orb just",
    "        // swallows the click, exactly like the Hitpoints orb. lclite:stat-orbs",
    "        if (spec !== null && statOrbsOrbHit(mx, my, L.specX + L.specR, L.specY + L.specR, L.specR)) {",
    "            if (S.specClick && spec.click >= 0 && statOrbsSpecWeapon(IfType.list, spec)) {",
    "                this.orbsSpecToggle(spec);",
    "            }",
    "",
    "            this.mouseClickButton = 0;",
    "            return true;",
    "        }",
    "",
    "        if (!this.orbsPanelOpen || this.orbsBook === null) {",
    "            return false;",
    "        }",
    "",
    "        const box: number[] = statOrbsBookBox(L.ox + L.orbLeft + 2 * L.r);",
    "        const cell: number = statOrbsBookHit(mx, my, box);",
    "        if (cell >= 0) {",
    "            this.orbsPrayerClick(this.orbsBook.cells[cell]);",
    "            this.mouseClickButton = 0;  // one prayer per click; the book stays open",
    "            return true;",
    "        }",
    "",
    "        if (mx >= 0 && my >= 0 && mx < ORB_PANEL_W && my < ORB_PANEL_H) {",
    "            // a click anywhere else ON the panel closes the book and is consumed with it:",
    "            // a dropdown, not a window you have to find the orb for again. Clicks outside",
    "            // the panel are the engine's, so walking and everything else still work.",
    "            this.orbsPanelOpen = false;",
    "            this.mouseClickButton = 0;",
    "            return true;",
    "        }",
    "",
    "        return false;",
    "    }",
    "",
    "    /// custom: toggle run by sending the options tab's OWN run/walk button click. The",
    "    /// client sends exactly the packet that button sends, so there is no special case for",
    "    /// the server to know about: the controls tab sits in player.tabs from login, which",
    "    /// is what makes its components visible to the IfButtonHandler. The varp is flipped",
    "    /// optimistically the way the client's own button paths flip theirs, and the",
    "    /// `%option_run = %option_run;` resync at the end of both run triggers corrects it on",
    "    /// the next tick if the click was refused (under 1% energy). lclite:stat-orbs",
    "    private orbsRunToggle(): void {",
    "        const run: OrbRun | null = this.orbsRun;",
    "        if (run === null) {",
    "            return;",
    "        }",
    "",
    "        const on: boolean = this.var[run.varp] === 1;",
    "        this.var[run.varp] = on ? 0 : 1;",
    "        this.clientVar(run.varp);",
    "        this.redrawSide = true;",
    "",
    "        this.out.p1Enc(ClientProt.IF_BUTTON);",
    "        this.out.p2(on ? run.off : run.on);",
    "    }",
    "",
    "    /// custom: arm (or disarm) the special attack by sending the COMBAT TAB'S OWN spec-bar",
    "    /// click — the very IF_BUTTON the player's own click on the bar's `Use Special Attack`",
    "    /// option sends, so there is nothing here for the server to special-case, and the",
    "    /// bar's own label lights up along with this orb.",
    "    ///",
    "    /// Nothing is flipped optimistically here, unlike run and the prayers: `sa_attack` has",
    "    /// no `%sa_attack = %sa_attack;` resync at the end of its trigger, so a flip the server",
    "    /// refused (no energy left — `sa_enough_energy` clears the flag) would have no",
    "    /// correction to arrive afterwards and the orb would sit armed over a server that",
    "    /// disagrees. The varp is `transmit=yes`, so the orb lights up from the server's own",
    "    /// update instead, a tick later. lclite:stat-orbs",
    "    private orbsSpecToggle(spec: OrbSpec): void {",
    "        if (spec.click < 0) {",
    "            return;",
    "        }",
    "",
    "        this.out.p1Enc(ClientProt.IF_BUTTON);",
    "        this.out.p2(spec.click);",
    "    }",
    "",
    "    /// custom: one prayer, from the book on the minimap — the same IF_BUTTON the prayer",
    "    /// tab's own button sends, with the same optimistic varp flip that path makes. Every",
    "    /// prayer trigger ends with a `%prayerN = %prayerN;` resync, so a refusal (no points,",
    "    /// level too low) or a conflicting-prayer shutdown is corrected on the next tick.",
    "    /// lclite:stat-orbs",
    "    private orbsPrayerClick(cell: OrbBookCell): void {",
    "        this.var[cell.varp] = this.var[cell.varp] === 1 ? 0 : 1;",
    "        this.clientVar(cell.varp);",
    "        this.redrawSide = true;",
    "",
    "        this.out.p1Enc(ClientProt.IF_BUTTON);",
    "        this.out.p2(cell.click);",
    "    }",
    "",
    "    /// custom: the quick-prayer book, painted into the same widget buffer as the orbs,",
    "    /// from the GAME'S OWN prayer art: each cell blits its icon component's `graphic`",
    "    /// (unlit) or `graphic2` (lit — the component's own script IS the level requirement,",
    "    /// and getIfActive() already answers it), and the toggle's active graphic — the",
    "    /// prayer glow — goes over the top while that prayer is on. So the book can never",
    "    /// disagree with the prayer tab, and this mod ships no art at all. lclite:stat-orbs",
    "    private drawOrbBook(box: number[]): void {",
    "        const book: OrbBook | null = this.orbsBook;",
    "        if (book === null) {",
    "            return;",
    "        }",
    "",
    "        // 2004 interface furniture: a hard 1px outline, a bevel lit on the top and left",
    "        // and shadowed on the bottom and right, and the stone the side panels are made of.",
    "        // Every pixel is opaque — the stone left of the map window is painted once at boot",
    "        // and never re-cleared, so a blend there would accumulate and darken each frame.",
    "        const x: number = box[0];",
    "        const y: number = box[1];",
    "        const w: number = box[2];",
    "        const h: number = box[3];",
    "        Pix2D.fillRect(x, y, w, h, 0x564d42);",
    "        Pix2D.drawRect(x, y, w, h, 0x000000);",
    "        Pix2D.hline(x + 1, y + 1, w - 2, 0x6e6659);",
    "        Pix2D.vline(x + 1, y + 1, h - 2, 0x6e6659);",
    "        Pix2D.hline(x + 1, y + h - 2, w - 2, 0x2a251f);",
    "        Pix2D.vline(x + w - 2, y + 1, h - 2, 0x2a251f);",
    "",
    "        // The icons ride Pix32.plotSprite, which skips transparent pixels and clips to",
    "        // Pix2D's current rect. Nothing else on this path sets a rect (drawInterface is",
    "        // the only thing that does, and it is not called from minimapDraw), but the widget",
    "        // rebinds itself at the top of the frame — so set it explicitly and let",
    "        // minimapDraw's tail rebind areaGame as it already does.",
    "        Pix2D.setClipping(0, 0, ORB_PANEL_W, ORB_PANEL_H);",
    "",
    "        for (let i: number = 0; i < book.cells.length; i++) {",
    "            const cell = book.cells[i];",
    "            const at: number[] = statOrbsBookCell(box, i);",
    "            const icon: Pix32 | null = this.getIfActive(cell.icon as IfType) ? (cell.icon.graphic2 as Pix32 | null) : (cell.icon.graphic as Pix32 | null);",
    "            if (icon !== null) {",
    "                icon.plotSprite(at[0], at[1]);",
    "            }",
    "",
    "            if (this.orbsBookHover === i) {",
    "                Pix2D.drawRect(at[0] - 1, at[1] - 1, 32, 32, 0xd8cfa8);",
    "            }",
    "        }",
    "",
    "        // the glow in a second pass, so a neighbour's hover box can never sit on top of an",
    "        // active prayer's own highlight",
    "        for (let i: number = 0; i < book.cells.length; i++) {",
    "            const cell = book.cells[i];",
    "            if (this.var[cell.varp] !== 1) {",
    "                continue;",
    "            }",
    "",
    "            const toggle: IfType | null = IfType.list[cell.click] ?? null;",
    "            const glow: Pix32 | null = toggle === null ? null : (toggle.graphic2 as Pix32 | null);",
    "            if (glow === null) {",
    "                continue;",
    "            }",
    "",
    "            const at: number[] = statOrbsBookCell(box, i);",
    "            // the glow sprite is the toggle's own active graphic, so it lands where the",
    "            // prayer tab draws it: centred on the 34x34 button, 2px outside the icon.",
    "            glow.plotSprite(at[0] - 2, at[1] - 2);",
    "        }",
    "    }",
    "",
    "    /// custom: a hard 1px ring at radius `rr` around (cx, cy) — the special attack orb's",
    "    /// ARMED highlight. 2004 has no glow: a lit state is a lit RIM, the same trick the",
    "    /// options tab's own buttons pull with their active graphic. lclite:stat-orbs",
    "    private static orbRing(cx: number, cy: number, rr: number, rgb: number): void {",
    "        const lo: number = (rr - 0.5) * (rr - 0.5);",
    "        const hi: number = (rr + 0.5) * (rr + 0.5);",
    "        for (let dy: number = -rr; dy <= rr; dy++) {",
    "            for (let dx: number = -rr; dx <= rr; dx++) {",
    "                const d2: number = dx * dx + dy * dy;",
    "                if (d2 >= lo && d2 < hi) {",
    "                    Client.orbPixel(cx + dx, cy + dy, rgb);",
    "                }",
    "            }",
    "        }",
    "    }",
    "",
    "    /// custom: bounds-checked single pixel into the bound buffer (the orbs straddle the",
    "    /// widget edge when dragged there, and Pix2D.pixels has no clipping of its own). With",
    "    /// a clip disc it also refuses anything outside it — an orb's mark is drawn into a",
    "    /// CIRCULAR glass, and its square corners must not land on the metal rim.",
    "    /// lclite:stat-orbs",
    "    private static orbPixel(x: number, y: number, rgb: number, clipR2: number = -1, cx: number = 0, cy: number = 0): void {",
    "        if (x < 0 || y < 0 || x >= Pix2D.width || y >= Pix2D.height) {",
    "            return;",
    "        }",
    "",
    "        if (clipR2 >= 0) {",
    "            const dx: number = x - cx;",
    "            const dy: number = y - cy;",
    "            if (dx * dx + dy * dy > clipR2) {",
    "                return;",
    "            }",
    "        }",
    "",
    "        Pix2D.pixels[x + y * Pix2D.width] = rgb;",
    "    }",
    "",
    "    /// custom: '#rrggbb' -> 0xrrggbb, falling back for anything malformed",
    "    private static orbColour(key: string, def: number): number {",
    "        const v: string = localStorage.getItem(key) || '';",
    "        if (v.length !== 7 || v.charAt(0) !== '#') {",
    "            return def;",
    "        }",
    "        const n: number = parseInt(v.substring(1), 16);",
    "        return isNaN(n) ? def : n;",
    "    }",
    "",
    "    /// custom: multiply a colour's channels (f > 1 lightens, f < 1 darkens), clamped —",
    "    /// how a 2004 palette gets a shade without a gradient",
    "    private static orbShade(rgb: number, f: number): number {",
    "        const r: number = Math.min(255, ((((rgb >> 16) & 0xff) * f) | 0));",
    "        const g: number = Math.min(255, ((((rgb >> 8) & 0xff) * f) | 0));",
    "        const b: number = Math.min(255, (((rgb & 0xff) * f) | 0));",
    "        return (r << 16) | (g << 8) | b;",
    "    }",
    "",
    "    // todo: order",
    "    private gameDraw(): void {"
   ],
   "note": "src/client/Client.ts @ old line 3888",
   "occ": 1,
   "pos": [
    3888,
    0,
    3886,
    3889
   ]
  },
  {
   "find": [
    "        this.areaMap?.setPixels();",
    "",
    "        if (this.minimapState == 2) {",
    "            if (this.mapback !== null) {"
   ],
   "replace": [
    "        this.areaMap?.setPixels();",
    "",
    "        // custom (lclite \"stat-orbs\"): read our OWN key here (runs every frame); lclite:stat-orbs",
    "        // one-time wipe of orb pixels left in the widget buffer after the",
    "        // toggle goes off mid-session (the stone strip is not redrawn per tick)",
    "        this.orbsEnabled = localStorage.getItem('statOrbs') === 'true';",
    "        if (this.orbsWereOn && !this.orbsEnabled) {",
    "            this.orbsWereOn = false;",
    "            this.orbsPanelOpen = false;     // the book goes with the orbs it hangs off",
    "            this.orbsBookHover = -1;",
    "            this.mapback?.plotSprite(0, 0);",
    "            this.orbsStripClear();          // ...and the stone strip the orbs may have hung in",
    "        }",
    "",
    "        if (this.minimapState == 2) {",
    "            if (this.mapback !== null) {"
   ],
   "note": "src/client/Client.ts @ old line 11334",
   "occ": 1,
   "pos": [
    11334,
    0,
    11332,
    11335
   ]
  },
  {
   "find": [
    "            this.compass?.scanlineRotatePlotSprite(0, 0, 33, 33, 25, 25, this.orbitCameraYaw, 256, this.compassMaskLineOffsets, this.compassMaskLineLengths);",
    "",
    "            this.areaGame?.setPixels();",
    "            return;"
   ],
   "replace": [
    "            this.compass?.scanlineRotatePlotSprite(0, 0, 33, 33, 25, 25, this.orbitCameraYaw, 256, this.compassMaskLineOffsets, this.compassMaskLineLengths);",
    "",
    "            // custom: orbs survive the hidden-minimap state too lclite:stat-orbs",
    "            if (this.orbsEnabled) {",
    "                this.drawStatOrbs();",
    "                this.orbsWereOn = true;",
    "            }",
    "",
    "            this.areaGame?.setPixels();",
    "            return;"
   ],
   "note": "src/client/Client.ts @ old line 11348",
   "occ": 1,
   "pos": [
    11348,
    0,
    11346,
    11349
   ]
  },
  {
   "find": [
    "        Pix2D.fillRect(97, 78, 3, 3, Colour.WHITE);",
    "",
    "        this.areaGame?.setPixels();",
    "    }"
   ],
   "replace": [
    "        Pix2D.fillRect(97, 78, 3, 3, Colour.WHITE);",
    "",
    "        // custom: OSRS-style stat orbs (lclite TYPE B hook — see lclite/docs/MODS.md) lclite:stat-orbs",
    "        // drawn into the minimap widget buffer so they ride ON TOP of the interface",
    "        if (this.orbsEnabled) {",
    "            this.drawStatOrbs();",
    "            this.orbsWereOn = true;",
    "        }",
    "",
    "        this.areaGame?.setPixels();",
    "    }"
   ],
   "note": "src/client/Client.ts @ old line 11449",
   "occ": 1,
   "pos": [
    11449,
    0,
    11447,
    11450
   ]
  }
 ]
}