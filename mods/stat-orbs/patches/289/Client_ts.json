{
 "file": "webclient/src/client/Client.ts",
 "mod": "stat-orbs",
 "rev": "289",
 "generated_from": {
  "repo": "webclient",
  "head": "a52b321a9f42530f3dd2f1a02cd335c955791a12"
 },
 "hunks": [
  {
   "find": [
    "import IfType from '#/config/IfType.js';",
    "import { ComponentType, ButtonType } from '#/config/IfType.js';",
    "",
    "import ClientEntity from '#/dash3d/ClientEntity.js';"
   ],
   "replace": [
    "import IfType from '#/config/IfType.js';",
    "import { ComponentType, ButtonType } from '#/config/IfType.js';",
    "// lclite:stat-orbs",
    "import type { OrbBook, OrbBookCell, OrbRun } from '#/client/StatOrbs.js';",
    "import {",
    "    ORB_ORIGIN_X, ORB_ORIGIN_Y, ORB_PANEL_H, ORB_PANEL_W, ORB_RUN_CLIENTCODE,",
    "    statOrbsBook, statOrbsBookBox, statOrbsBookCell, statOrbsBookHit, statOrbsDigitAdvance,",
    "    statOrbsInBox, statOrbsNumberLeft, statOrbsNumberTop, statOrbsNumberWidth, statOrbsOrbHit,",
    "    statOrbsOrbLeft, statOrbsRun, statOrbsScale, statOrbsSettings",
    "} from '#/client/StatOrbs.js'; // custom (lclite \"stat-orbs\" mod): the settings parse, the prayer-book and run-button lookup, the quick-prayer panel's geometry",
    "",
    "import ClientEntity from '#/dash3d/ClientEntity.js';"
   ],
   "note": "src/client/Client.ts @ old line 23",
   "occ": 1,
   "pos": [
    23,
    0,
    21,
    24
   ]
  },
  {
   "find": [
    "",
    "    minimapLoop(): void {",
    "        if (this.minimapState !== 0 || this.mouseClickButton !== 1 || !this.localPlayer) {",
    "            return;"
   ],
   "replace": [
    "",
    "    minimapLoop(): void {",
    "        // custom (lclite \"stat-orbs\" mod): the orb column and its prayer book sit ON the",
    "        // minimap panel, so a click that lands on one of them is not a walk. The orbs' own",
    "        // hook runs here, at the top of the engine's minimap click handling — and inside",
    "        // checkClickInput's guard, because minimapLoop is only reached through it — and",
    "        // consumes the click before the walk below can claim it. A click anywhere else on",
    "        // the panel is left to the engine, so walking the minimap is untouched.",
    "        // lclite:stat-orbs",
    "        if (this.orbsClickLoop()) {",
    "            return;",
    "        }",
    "",
    "        if (this.minimapState !== 0 || this.mouseClickButton !== 1 || !this.localPlayer) {",
    "            return;"
   ],
   "note": "src/client/Client.ts @ old line 2743",
   "occ": 1,
   "pos": [
    2743,
    0,
    2741,
    2744
   ]
  },
  {
   "find": [
    "    }",
    "",
    "    // todo: order",
    "    private gameDraw(): void {"
   ],
   "replace": [
    "    }",
    "",
    "    /// custom (lclite \"stat-orbs\" mod): fields + tunables live in this one lclite:stat-orbs",
    "    /// isolated slot (far from any other mod's hunks) so regen routes the whole",
    "    /// block to mods/stat-orbs; the three minimapDraw hooks stay separate hunks.",
    "    /// OSRS-style Hitpoints / Prayer / Run-energy data orbs on the minimap panel.",
    "    public orbsEnabled: boolean = false;",
    "    private orbsWereOn: boolean = false;",
    "    /// signature of the layout drawn last frame: a change (moved, resized, recoloured)",
    "    /// leaves stale orb pixels in the widget buffer, so the mapback is re-plotted once",
    "    /// to wipe them — see drawStatOrbs.",
    "    private orbsSig: string = '';",
    "    private orbsApiBound: boolean = false;",
    "",
    "    /// custom: the prayer book and the run/walk buttons, looked up in the LOADED",
    "    /// interface list (IfType.list is complete before login) — see orbsResolve(). Both",
    "    /// stay null when this revision's interfaces do not match, and the panel and the",
    "    /// run click are then simply not offered. lclite:stat-orbs",
    "    private orbsBook: OrbBook | null = null;",
    "    private orbsRun: OrbRun | null = null;",
    "    private orbsBookTried: boolean = false;",
    "    /// the quick-prayer panel's open state. Never persisted: a reload starts closed,",
    "    /// and switching the mod off closes it. orbsBookHover is the cell the cursor is on",
    "    /// (-1 for none) — the panel's only hover feedback. lclite:stat-orbs",
    "    private orbsPanelOpen: boolean = false;",
    "    private orbsBookHover: number = -1;",
    "",
    "    /// custom: 3x5 pixel digits for the orb readouts (row-major, '1' = ink). The minimap",
    "    /// panel's left stone strip is only ~25px wide, so no 2004 font fits to the LEFT of",
    "    /// an orb (p12 digits are 6px wide plus a shadow — three of them are 18px); this",
    "    /// tiny font can, and a tiny font is what OSRS itself draws its orb numbers in.",
    "    static readonly ORB_DIGITS: string[] = [",
    "        '111101101101111', // 0",
    "        '010110010010111', // 1",
    "        '111001111100111', // 2",
    "        '111001111001111', // 3",
    "        '101101111001001', // 4",
    "        '111100111001111', // 5",
    "        '111100111101111', // 6",
    "        '111001001010010', // 7",
    "        '111101111101111', // 8",
    "        '111101111001111'  // 9",
    "    ];",
    "    /// custom: 7x7 orb glyphs (row-major, '1' = ink) — heart, prayer star, and the run",
    "    /// bolt. The run mark is a LIGHTNING BOLT, not the blob it used to be: 2004 has no",
    "    /// run icon of its own to borrow (the options tab's run button is a plain button),",
    "    /// so the orb carries the one mark that reads as \"energy\" at 7x7. lclite:stat-orbs",
    "    static readonly ORB_GLYPH_HP: string = '0110110111111111111110111110001110000010000000000';",
    "    static readonly ORB_GLYPH_PRAYER: string = '0001000000100001111101111111011111000010000001000';",
    "    static readonly ORB_GLYPH_RUN: string = '0000110000110000110000011111000011000001100000110000';",
    "    /// custom: the 9 anchor points of the panel's alt-drag placement, as a POSITIONAL",
    "    /// table. The drag layer stores the anchor's NAME ('MC') in localStorage, and a",
    "    /// runtime string must never be an object-literal KEY in bundled code: terser's",
    "    /// property mangler renames the keys (`{MC: …}` ships as `{av: …}`) so every",
    "    /// lookup with the stored name misses silently — which is exactly what kept canvas",
    "    /// surfaces from ever moving. Names and fractions are parallel arrays, matched by",
    "    /// index. lclite:stat-orbs",
    "    static readonly ORB_ANCH_NAMES: string[] = ['TL', 'TC', 'TR', 'ML', 'MC', 'MR', 'BL', 'BC', 'BR'];",
    "    static readonly ORB_ANCH: number[][] = [[0, 0], [0.5, 0], [1, 0], [0, 0.5], [0.5, 0.5], [1, 0.5], [0, 1], [0.5, 1], [1, 1]];",
    "",
    "    /// custom: the orb column's live geometry. Every key is read HERE, every frame, at",
    "    /// this mod's own hook (rule 5) — including the lcmStatOrbs* placement keys, which",
    "    /// only the panel's drag layer writes (alt+drag; the panel hit-tests the box this",
    "    /// returns through window.lcmStatOrbsBounds). lclite:stat-orbs",
    "    private orbLayout(): { r: number; orbLeft: number; step: number; ox: number; oy: number; boxW: number; boxH: number; numbers: string; scale: number; pie: boolean; pulse: boolean } {",
    "        const S = statOrbsSettings((key: string) => localStorage.getItem(key));",
    "        const size: number = Math.max(20, Math.min(28, parseInt(localStorage.getItem('statOrbsSize') || '') || 22));",
    "        const r: number = (size / 2) | 0;",
    "        const numbers: string = localStorage.getItem('statOrbsNumbers') || 'left';",
    "        // 'left' readouts reserve the strip for three digits at the chosen SCALE (14px at",
    "        // 1x, the shipped default — see statOrbsOrbLeft); the other two modes have no text",
    "        // beside the orbs, so they hug the frame edge. Bigger digits pay for themselves by",
    "        // pushing the whole column further right, over the map's own left edge.",
    "        const orbLeft: number = statOrbsOrbLeft(numbers, S.numberScale);",
    "        // even spread down the strip: the compass owns y < 33 and the widget ends at 156,",
    "        // so the three orbs share y 34..154 whatever their size (step = 60 - r).",
    "        const step: number = 60 - r;",
    "        const boxW: number = orbLeft + 2 * r;",
    "        const boxH: number = 2 * r + 2 * step;",
    "        let ox: number = 0;",
    "        let oy: number = 34;",
    "        const ai: number = Client.ORB_ANCH_NAMES.indexOf(localStorage.getItem('lcmStatOrbsAnchor') || '');",
    "        if (ai >= 0) {",
    "            const k: number[] = Client.ORB_ANCH[ai];",
    "            const off: string[] = (localStorage.getItem('lcmStatOrbsOffset') || '0,0').split(',');",
    "            ox = Math.round(172 * k[0]) + (parseInt(off[0]) || 0);",
    "            oy = Math.round(156 * k[1]) + (parseInt(off[1]) || 0);",
    "        }",
    "        // clamped FLUSH (no margin): the widget itself is the boundary, so a canvas",
    "        // surface can never be lost off-screen — the panel clamps the same way.",
    "        ox = Math.max(0, Math.min(ox, 172 - boxW));",
    "        oy = Math.max(0, Math.min(oy, 156 - boxH));",
    "        return { r: r, orbLeft: orbLeft, step: step, ox: ox, oy: oy, boxW: boxW, boxH: boxH, numbers: numbers, scale: S.numberScale, pie: localStorage.getItem('statOrbsFill') === 'pie', pulse: localStorage.getItem('statOrbsPulse') !== 'false' };",
    "    }",
    "",
    "    /// custom (lclite \"stat-orbs\" mod): the three data orbs, painted into the areaMap",
    "    /// widget buffer (172x156, composited at canvas 550,4) at the END of minimapDraw()",
    "    /// — the same ride as the minimap itself, so they land ON TOP of the interface and",
    "    /// the gpu mod's overlay upload picks them up like every other HUD pixel. Every",
    "    /// pixel written is opaque: the stone strip left of the map window is painted once",
    "    /// at boot and never re-cleared, so an alpha blend there would accumulate and",
    "    /// darken it a little more every tick.",
    "    private drawStatOrbs(): void {",
    "        const L = this.orbLayout();",
    "        const hpCur: number = this.statEffectiveLevel[3];",
    "        const hpMax: number = this.statBaseLevel[3];",
    "        const prCur: number = this.statEffectiveLevel[5];",
    "        const prMax: number = this.statBaseLevel[5];",
    "        const hpFrac: number = hpMax > 0 ? hpCur / hpMax : 0;",
    "        const prFrac: number = prMax > 0 ? prCur / prMax : 0;",
    "        // runenergy arrives as energy/100 on the wire (0-100)",
    "        const rnFrac: number = Math.max(0, Math.min(1, this.runenergy / 100));",
    "        const hpRgb: number = Client.orbColour('statOrbsHpColor', 0xe82623);",
    "        const prRgb: number = Client.orbColour('statOrbsPrayerColor', 0xd9a318);",
    "        const rnRgb: number = Client.orbColour('statOrbsRunColor', 0x71c8e8);",
    "",
    "        // stale-pixel wipe, only when what was drawn actually changed: moving, resizing",
    "        // or recolouring the orbs abandons pixels on stone that is never repainted — and",
    "        // so does a changed READOUT, whose glyphs change width with the value (100 -> 45",
    "        // leaves a stale third digit behind) and whose colour follows the HP fraction.",
    "        // The mapback's map window is a transparent hole, so re-plotting it restores the",
    "        // stone and leaves the map alone (minimapDraw re-blits the map this same tick).",
    "        //",
    "        // The quick-prayer panel adds state that moves no number at all: a prayer",
    "        // toggling, the prayer level unlocking an icon, the panel opening or closing, the",
    "        // cursor crossing onto another cell. All of it is in the signature, or the panel",
    "        // would show yesterday's prayers. lclite:stat-orbs",
    "        this.orbsResolve();",
    "        const runOn: boolean = this.orbsRun !== null && this.var[this.orbsRun.varp] === 1;",
    "        const bookBox: number[] = statOrbsBookBox(L.ox + L.orbLeft + 2 * L.r);",
    "        // the cursor's cell, straight from the live canvas position, so the hover box is",
    "        // already right for the frame being drawn. lclite:stat-orbs",
    "        this.orbsBookHover = this.orbsPanelOpen && this.orbsBook !== null ? statOrbsBookHit(this.mouseX - ORB_ORIGIN_X, this.mouseY - ORB_ORIGIN_Y, bookBox) : -1;",
    "        let bookSig: string = '0';",
    "        if (this.orbsBook !== null) {",
    "            let active: number = 0;",
    "            for (let i: number = 0; i < this.orbsBook.cells.length; i++) {",
    "                if (this.var[this.orbsBook.cells[i].varp] === 1) {",
    "                    active |= 1 << i;",
    "                }",
    "            }",
    "            bookSig = (this.orbsPanelOpen ? '1' : '0') + ',' + active + ',' + this.statBaseLevel[5] + ',' + bookBox[0] + ',' + this.orbsBookHover;",
    "        }",
    "        const sig: string = L.ox + ',' + L.oy + ',' + L.r + ',' + L.numbers + ',' + L.scale + ',' + (L.pie ? 'p' : 'l') + ',' + hpRgb + ',' + prRgb + ',' + rnRgb + ',' + hpCur + ',' + prCur + ',' + this.runenergy + ',' + (runOn ? 1 : 0) + ',' + bookSig;",
    "        if (sig !== this.orbsSig) {",
    "            if (this.orbsSig !== '') {",
    "                this.mapback?.plotSprite(0, 0);",
    "            }",
    "            this.orbsSig = sig;",
    "        }",
    "",
    "        const cx: number = L.ox + L.orbLeft + L.r;",
    "        const cy0: number = L.oy + L.r;",
    "",
    "        // The prayer book goes UNDER the orbs. At 3x the readouts reach across the",
    "        // panel's own left edge, and a prayer book you cannot see your prayer points",
    "        // through is worse than one icon behind an orb. lclite:stat-orbs",
    "        if (this.orbsPanelOpen && this.orbsBook !== null) {",
    "            this.drawOrbBook(bookBox);",
    "        }",
    "",
    "        // OSRS order, top to bottom: Hitpoints, Prayer, Run energy. lclite:stat-orbs",
    "        const orbs: { cy: number; rgb: number; frac: number; value: number; textRgb: number; glyph: string }[] = [",
    "            { cy: cy0, rgb: hpRgb, frac: hpFrac, value: hpCur, textRgb: hpFrac > 0.66 ? Colour.GREEN : hpFrac > 0.33 ? Colour.YELLOW : Colour.RED, glyph: Client.ORB_GLYPH_HP },",
    "            { cy: cy0 + L.step, rgb: prRgb, frac: prFrac, value: prCur, textRgb: Colour.WHITE, glyph: Client.ORB_GLYPH_PRAYER },",
    "            { cy: cy0 + 2 * L.step, rgb: rnRgb, frac: rnFrac, value: this.runenergy, textRgb: Colour.WHITE, glyph: Client.ORB_GLYPH_RUN }",
    "        ];",
    "",
    "        for (let i: number = 0; i < orbs.length; i++) {",
    "            const orb = orbs[i];",
    "            // OSRS low-hitpoints warning: flash the orb while health is nearly gone",
    "            let rgb: number = L.pulse && orb.frac > 0 && orb.frac <= 0.25 && Client.loopCycle % 16 < 8 ? Client.orbShade(orb.rgb, 1.7) : orb.rgb;",
    "            // RUN ON: the run orb's glass lightens, so the toggle's own state is readable",
    "            // at a glance — the same job the options tab's run button does with its",
    "            // active graphic, and the only feedback a one-click toggle can give.",
    "            // lclite:stat-orbs",
    "            if (i === 2 && runOn) {",
    "                rgb = Client.orbShade(rgb, 1.35);",
    "            }",
    "            this.drawOrb(cx, orb.cy, L.r, rgb, orb.frac, L.pie, orb.glyph);",
    "",
    "            const text: string = String(orb.value);",
    "            if (L.numbers === 'left') {",
    "                this.drawOrbNumber(text, statOrbsNumberLeft(cx, L.r, text, L.scale), orb.cy, orb.textRgb, L.scale);",
    "            } else if (L.numbers === 'inside') {",
    "                this.drawOrbNumber(text, cx - (statOrbsNumberWidth(text, L.scale) >> 1), orb.cy, orb.textRgb, L.scale);",
    "            }",
    "        }",
    "",
    "        // placement bridge for the panel's drag layer (positional arrays only — object",
    "        // literals arrive key-mangled across the bundle boundary). Bound once; the box",
    "        // stays live, and reports null while the mod is off so alt+drag cannot grab it.",
    "        const W: any = window as any;",
    "        if (!this.orbsApiBound && W['lcmAnchor']) {",
    "            this.orbsApiBound = true;",
    "            W['lcmStatOrbsBounds'] = (): number[] | null => {",
    "                const b = this.orbLayout();",
    "                return this.orbsEnabled ? [b.ox, b.oy, b.boxW, b.boxH] : null;",
    "            };",
    "            W['lcmAnchor']['registerCanvas']?.(['stat-orbs', 'lcmStatOrbsAnchor', 'lcmStatOrbsOffset', 'TL', '0,34', () => (W['lcmStatOrbsBounds'] ? W['lcmStatOrbsBounds']() : null), [550, 4, 172, 156, 172, 156]]);",
    "        }",
    "    }",
    "",
    "    /// custom: one procedural orb. Hard 1px black outline, a bevelled stone rim in the",
    "    /// mapback's own palette (lit upper-left, shadowed lower-right), a dark glass body,",
    "    /// and a liquid (or clockwise pie) fill shaded in three HARD bands. No anti-aliasing",
    "    /// and no smooth gradient anywhere: the 2004/5 interface is built from discrete",
    "    /// pixel steps, and a gradient is exactly what made the old orbs read as modern",
    "    /// vector art. The stat glyph sits in the middle with a 1px outline and the gloss",
    "    /// spark on the upper-left shoulder, which is where OSRS puts it too.",
    "    private drawOrb(cx: number, cy: number, r: number, rgb: number, frac: number, pie: boolean, glyph: string): void {",
    "        if (frac < 0) {",
    "            frac = 0;",
    "        } else if (frac > 1) {",
    "            frac = 1;",
    "        }",
    "",
    "        const rim: number = r >= 9 ? 2 : 1;                       // stone frame thickness",
    "        const innerR: number = r - 1 - rim;                       // radius of the glass",
    "        const tau: number = 6.283185307179586;",
    "        const sweep: number = tau * frac;",
    "        const surface: number = cy + innerR - 2 * innerR * frac;  // liquid level (liquid fill)",
    "        const dst: Int32Array = Pix2D.pixels;",
    "        const w: number = Pix2D.width;",
    "        const rr: number = r * r;",
    "        const ir: number = innerR * innerR;",
    "        const rimIn: number = (r - 1) * (r - 1);",
    "",
    "        for (let dy: number = -r; dy <= r; dy++) {",
    "            const y: number = cy + dy;",
    "            if (y < 0 || y >= Pix2D.height) {",
    "                continue;",
    "            }",
    "",
    "            for (let dx: number = -r; dx <= r; dx++) {",
    "                const x: number = cx + dx;",
    "                if (x < 0 || x >= w) {",
    "                    continue;",
    "                }",
    "",
    "                const d2: number = dx * dx + dy * dy;",
    "                if (d2 > rr) {",
    "                    continue;",
    "                }",
    "",
    "                let colour: number;",
    "                if (d2 > rimIn) {",
    "                    colour = 0x0a0a0a;                            // hard outer outline",
    "                } else if (d2 > ir) {",
    "                    // bevelled metal rim: lit along the TOP half, shadowed below. A",
    "                    // diagonal split read as one flat dark ring against the stone frame,",
    "                    // and the old colours were too close to the stone to see at all.",
    "                    colour = dy < 0 ? 0x7d7466 : 0x2a251f;",
    "                } else {",
    "                    let filled: boolean;",
    "                    if (pie) {",
    "                        // angle clockwise from 12 o'clock (centre pixel: pin it filled)",
    "                        let ang: number = d2 === 0 ? 0 : Math.atan2(dx, -dy);",
    "                        if (ang < 0) {",
    "                            ang += tau;",
    "                        }",
    "                        filled = ang < sweep;",
    "                    } else {",
    "                        filled = y >= surface;",
    "                    }",
    "",
    "                    const d: number = Math.sqrt(d2);",
    "                    if (filled) {",
    "                        const t: number = d / innerR;",
    "                        colour = t > 0.84 ? Client.orbShade(rgb, 0.62) : t > 0.55 ? Client.orbShade(rgb, 0.82) : rgb;",
    "                        if (!pie && y - surface < 1) {",
    "                            colour = Client.orbShade(rgb, 1.45);   // meniscus line",
    "                        }",
    "                    } else {",
    "                        colour = d > innerR - 1.5 ? 0x2b2721 : 0x161310;",
    "                    }",
    "                }",
    "",
    "                dst[x + y * w] = colour;",
    "            }",
    "        }",
    "",
    "        // stat glyph: two passes so the outline never eats the ink",
    "        if (glyph.length === 49 && innerR >= 7) {",
    "            const ink: number = Client.orbShade(rgb, 1.8);",
    "            const gx: number = cx - 3;",
    "            const gy: number = cy - 3;",
    "            for (let pass: number = 0; pass < 2; pass++) {",
    "                const col: number = pass === 0 ? 0x1c0f0c : ink;",
    "                for (let j: number = 0; j < 7; j++) {",
    "                    for (let i: number = 0; i < 7; i++) {",
    "                        if (glyph.charAt(j * 7 + i) !== '1') {",
    "                            continue;",
    "                        }",
    "                        if (pass === 0) {",
    "                            Client.orbPixel(gx + i - 1, gy + j, col);",
    "                            Client.orbPixel(gx + i + 1, gy + j, col);",
    "                            Client.orbPixel(gx + i, gy + j - 1, col);",
    "                            Client.orbPixel(gx + i, gy + j + 1, col);",
    "                        } else {",
    "                            Client.orbPixel(gx + i, gy + j, col);",
    "                        }",
    "                    }",
    "                }",
    "            }",
    "        }",
    "",
    "        // 2004 gloss: a small hard-edged glint on the upper-left shoulder — a rounded",
    "        // blob (corners cut), which reads as wet glass; a plus-shaped cluster reads as a",
    "        // UI sparkle sitting on top of the orb instead.",
    "        const hx: number = cx - innerR + 1;",
    "        const hy: number = cy - innerR + 1;",
    "        Client.orbPixel(hx + 1, hy, 0xf7f3e8);",
    "        Client.orbPixel(hx + 2, hy, 0xf7f3e8);",
    "        Client.orbPixel(hx, hy + 1, 0xf7f3e8);",
    "        Client.orbPixel(hx + 1, hy + 1, 0xffffff);",
    "        Client.orbPixel(hx + 2, hy + 1, 0xf7f3e8);",
    "        Client.orbPixel(hx + 1, hy + 2, 0xf7f3e8);",
    "    }",
    "",
    "    /// custom: a readout in the tiny orb font — 4px advance, 5px tall, 1px black shadow",
    "    /// at scale 1 (the 2004 way of putting text on the stone frame), with every one of",
    "    /// those numbers multiplied by the SCALE setting: each glyph pixel becomes an s x s",
    "    /// block and the shadow moves a whole block, so the digits stay square and stay on",
    "    /// the pixel grid at 2x and 3x instead of smearing. x is the LEFT edge of the block;",
    "    /// the digits are drawn vertically centred on cy. lclite:stat-orbs",
    "    private drawOrbNumber(text: string, x: number, cy: number, rgb: number, scale: number): void {",
    "        const s: number = statOrbsScale(scale);",
    "        const y: number = statOrbsNumberTop(cy, s);",
    "        for (let pass: number = 0; pass < 2; pass++) {",
    "            const off: number = pass === 0 ? s : 0;",
    "            const col: number = pass === 0 ? 0x000000 : rgb;",
    "            for (let i: number = 0; i < text.length; i++) {",
    "                const d: number = text.charCodeAt(i) - 48;",
    "                if (d < 0 || d > 9) {",
    "                    continue;",
    "                }",
    "                const bits: string = Client.ORB_DIGITS[d];",
    "                for (let j: number = 0; j < 5; j++) {",
    "                    for (let k: number = 0; k < 3; k++) {",
    "                        if (bits.charAt(j * 3 + k) !== '1') {",
    "                            continue;",
    "                        }",
    "                        for (let dy: number = 0; dy < s; dy++) {",
    "                            for (let dx: number = 0; dx < s; dx++) {",
    "                                Client.orbPixel(x + i * statOrbsDigitAdvance(s) + k * s + dx + off, y + j * s + dy + off, col);",
    "                            }",
    "                        }",
    "                    }",
    "                }",
    "            }",
    "        }",
    "    }",
    "",
    "    /// custom: resolve the prayer book and the run/walk buttons out of the LOADED",
    "    /// interface list. IfType.list is filled from the interface jag before login, so this",
    "    /// normally answers on the first frame; while the list is still empty it simply",
    "    /// leaves both null and tries again next frame. Both lookups are STRUCTURAL (see",
    "    /// StatOrbs.ts) — no component id is hardcoded anywhere, because ids are assigned by",
    "    /// the interface packer and are a revision-specific accident. lclite:stat-orbs",
    "    private orbsResolve(): void {",
    "        if (this.orbsBook !== null && this.orbsRun !== null) {",
    "            return;",
    "        }",
    "",
    "        if (IfType.list.length === 0) {",
    "            return;",
    "        }",
    "",
    "        this.orbsBookTried = true;",
    "        this.orbsBook = statOrbsBook(IfType.list);",
    "",
    "        // the run varp is the one the cache marks clientcode 7 — the very test the server",
    "        // makes to find it (VarPlayerType.RUN), so this cannot drift from the game's own",
    "        // idea of which varp run energy lives in. lclite:stat-orbs",
    "        let runVarp: number = -1;",
    "        for (let i: number = 0; i < VarpType.list.length; i++) {",
    "            if (VarpType.list[i] && VarpType.list[i].clientcode === ORB_RUN_CLIENTCODE) {",
    "                runVarp = i;",
    "                break;",
    "            }",
    "        }",
    "        this.orbsRun = statOrbsRun(IfType.list, runVarp);",
    "    }",
    "",
    "    /// custom: the orb column's own click hook — the run orb's toggle, the prayer orb's",
    "    /// book, and the book's fifteen cells. It runs at the TOP of the engine's own minimap",
    "    /// click handling (minimapLoop, which the engine only reaches through mainloop's",
    "    /// checkClickInput guard) and CONSUMES the clicks it takes, so a click on an orb or on",
    "    /// the book can never also walk the minimap. Returns true when it took the click.",
    "    ///",
    "    /// It hangs off minimapLoop rather than the checkClickInput block on purpose: the wiki",
    "    /// button's hook already owns the only free insertion island there, and a second",
    "    /// insertion next to it would make the two mods one patch island — wiki-lookup's",
    "    /// corpus would then carry this mod's lines, and stripping wiki-lookup would take the",
    "    /// orbs' clicks with it. lclite:stat-orbs",
    "    private orbsClickLoop(): boolean {",
    "        if (!this.localPlayer || this.mouseClickButton !== 1) {",
    "            return false;",
    "        }",
    "",
    "        this.orbsEnabled = localStorage.getItem('statOrbs') === 'true';",
    "        if (!this.orbsEnabled) {",
    "            this.orbsPanelOpen = false;",
    "            this.orbsBookHover = -1;",
    "            return false;",
    "        }",
    "",
    "        this.orbsResolve();",
    "        const S = statOrbsSettings((key: string) => localStorage.getItem(key));",
    "        const L = this.orbLayout();",
    "        const mx: number = this.mouseClickX - ORB_ORIGIN_X;",
    "        const my: number = this.mouseClickY - ORB_ORIGIN_Y;",
    "        const cx: number = L.ox + L.orbLeft + L.r;",
    "",
    "        // the orbs themselves, in OSRS order: 0 Hitpoints (a readout — it does nothing,",
    "        // but it is still ours to swallow), 1 Prayer (the book), 2 Run energy (the toggle).",
    "        // All three consume: the orbs straddle the map window's own left edge, so half of",
    "        // each one would otherwise set a walk flag under the cursor.",
    "        for (let i: number = 0; i < 3; i++) {",
    "            if (!statOrbsOrbHit(mx, my, cx, L.oy + L.r + i * L.step, L.r)) {",
    "                continue;",
    "            }",
    "",
    "            if (i === 1 && S.prayerPanel && this.orbsBook !== null) {",
    "                this.orbsPanelOpen = !this.orbsPanelOpen;",
    "                this.mouseClickButton = 0;",
    "            } else if (i === 2 && S.runClick && this.orbsRun !== null) {",
    "                this.orbsRunToggle();",
    "                this.mouseClickButton = 0;",
    "            }",
    "",
    "            return true;                // an orb owns every click that lands on it",
    "        }",
    "",
    "        if (!this.orbsPanelOpen || this.orbsBook === null) {",
    "            return false;",
    "        }",
    "",
    "        const box: number[] = statOrbsBookBox(L.ox + L.orbLeft + 2 * L.r);",
    "        const cell: number = statOrbsBookHit(mx, my, box);",
    "        if (cell >= 0) {",
    "            this.orbsPrayerClick(this.orbsBook.cells[cell]);",
    "            this.mouseClickButton = 0;  // one prayer per click; the book stays open",
    "            return true;",
    "        }",
    "",
    "        if (mx >= 0 && my >= 0 && mx < ORB_PANEL_W && my < ORB_PANEL_H) {",
    "            // a click anywhere else ON the panel closes the book and is consumed with it:",
    "            // a dropdown, not a window you have to find the orb for again. Clicks outside",
    "            // the panel are the engine's, so walking and everything else still work.",
    "            this.orbsPanelOpen = false;",
    "            this.mouseClickButton = 0;",
    "            return true;",
    "        }",
    "",
    "        return false;",
    "    }",
    "",
    "    /// custom: toggle run by sending the options tab's OWN run/walk button click. The",
    "    /// client sends exactly the packet that button sends, so there is no special case for",
    "    /// the server to know about: the controls tab sits in player.tabs from login, which",
    "    /// is what makes its components visible to the IfButtonHandler. The varp is flipped",
    "    /// optimistically the way the client's own button paths flip theirs, and the",
    "    /// `%option_run = %option_run;` resync at the end of both run triggers corrects it on",
    "    /// the next tick if the click was refused (under 1% energy). lclite:stat-orbs",
    "    private orbsRunToggle(): void {",
    "        const run: OrbRun | null = this.orbsRun;",
    "        if (run === null) {",
    "            return;",
    "        }",
    "",
    "        const on: boolean = this.var[run.varp] === 1;",
    "        this.var[run.varp] = on ? 0 : 1;",
    "        this.clientVar(run.varp);",
    "        this.redrawSide = true;",
    "",
    "        this.out.p1Enc(ClientProt.IF_BUTTON);",
    "        this.out.p2(on ? run.off : run.on);",
    "    }",
    "",
    "    /// custom: one prayer, from the book on the minimap — the same IF_BUTTON the prayer",
    "    /// tab's own button sends, with the same optimistic varp flip that path makes. Every",
    "    /// prayer trigger ends with a `%prayerN = %prayerN;` resync, so a refusal (no points,",
    "    /// level too low) or a conflicting-prayer shutdown is corrected on the next tick.",
    "    /// lclite:stat-orbs",
    "    private orbsPrayerClick(cell: OrbBookCell): void {",
    "        this.var[cell.varp] = this.var[cell.varp] === 1 ? 0 : 1;",
    "        this.clientVar(cell.varp);",
    "        this.redrawSide = true;",
    "",
    "        this.out.p1Enc(ClientProt.IF_BUTTON);",
    "        this.out.p2(cell.click);",
    "    }",
    "",
    "    /// custom: the quick-prayer book, painted into the same widget buffer as the orbs,",
    "    /// from the GAME'S OWN prayer art: each cell blits its icon component's `graphic`",
    "    /// (unlit) or `graphic2` (lit — the component's own script IS the level requirement,",
    "    /// and getIfActive() already answers it), and the toggle's active graphic — the",
    "    /// prayer glow — goes over the top while that prayer is on. So the book can never",
    "    /// disagree with the prayer tab, and this mod ships no art at all. lclite:stat-orbs",
    "    private drawOrbBook(box: number[]): void {",
    "        const book: OrbBook | null = this.orbsBook;",
    "        if (book === null) {",
    "            return;",
    "        }",
    "",
    "        // 2004 interface furniture: a hard 1px outline, a bevel lit on the top and left",
    "        // and shadowed on the bottom and right, and the stone the side panels are made of.",
    "        // Every pixel is opaque — the stone left of the map window is painted once at boot",
    "        // and never re-cleared, so a blend there would accumulate and darken each frame.",
    "        const x: number = box[0];",
    "        const y: number = box[1];",
    "        const w: number = box[2];",
    "        const h: number = box[3];",
    "        Pix2D.fillRect(x, y, w, h, 0x564d42);",
    "        Pix2D.drawRect(x, y, w, h, 0x000000);",
    "        Pix2D.hline(x + 1, y + 1, w - 2, 0x6e6659);",
    "        Pix2D.vline(x + 1, y + 1, h - 2, 0x6e6659);",
    "        Pix2D.hline(x + 1, y + h - 2, w - 2, 0x2a251f);",
    "        Pix2D.vline(x + w - 2, y + 1, h - 2, 0x2a251f);",
    "",
    "        // The icons ride Pix32.plotSprite, which skips transparent pixels and clips to",
    "        // Pix2D's current rect. Nothing else on this path sets a rect (drawInterface is",
    "        // the only thing that does, and it is not called from minimapDraw), but the widget",
    "        // rebinds itself at the top of the frame — so set it explicitly and let",
    "        // minimapDraw's tail rebind areaGame as it already does.",
    "        Pix2D.setClipping(0, 0, ORB_PANEL_W, ORB_PANEL_H);",
    "",
    "        for (let i: number = 0; i < book.cells.length; i++) {",
    "            const cell = book.cells[i];",
    "            const at: number[] = statOrbsBookCell(box, i);",
    "            const icon: Pix32 | null = this.getIfActive(cell.icon as IfType) ? (cell.icon.graphic2 as Pix32 | null) : (cell.icon.graphic as Pix32 | null);",
    "            if (icon !== null) {",
    "                icon.plotSprite(at[0], at[1]);",
    "            }",
    "",
    "            if (this.orbsBookHover === i) {",
    "                Pix2D.drawRect(at[0] - 1, at[1] - 1, 32, 32, 0xd8cfa8);",
    "            }",
    "        }",
    "",
    "        // the glow in a second pass, so a neighbour's hover box can never sit on top of an",
    "        // active prayer's own highlight",
    "        for (let i: number = 0; i < book.cells.length; i++) {",
    "            const cell = book.cells[i];",
    "            if (this.var[cell.varp] !== 1) {",
    "                continue;",
    "            }",
    "",
    "            const toggle: IfType | null = IfType.list[cell.click] ?? null;",
    "            const glow: Pix32 | null = toggle === null ? null : (toggle.graphic2 as Pix32 | null);",
    "            if (glow === null) {",
    "                continue;",
    "            }",
    "",
    "            const at: number[] = statOrbsBookCell(box, i);",
    "            // the glow sprite is the toggle's own active graphic, so it lands where the",
    "            // prayer tab draws it: centred on the 34x34 button, 2px outside the icon.",
    "            glow.plotSprite(at[0] - 2, at[1] - 2);",
    "        }",
    "    }",
    "",
    "    /// custom: bounds-checked single pixel into the bound buffer (the orbs straddle the",
    "    /// widget edge when dragged there, and Pix2D.pixels has no clipping of its own)",
    "    private static orbPixel(x: number, y: number, rgb: number): void {",
    "        if (x < 0 || y < 0 || x >= Pix2D.width || y >= Pix2D.height) {",
    "            return;",
    "        }",
    "        Pix2D.pixels[x + y * Pix2D.width] = rgb;",
    "    }",
    "",
    "    /// custom: '#rrggbb' -> 0xrrggbb, falling back for anything malformed",
    "    private static orbColour(key: string, def: number): number {",
    "        const v: string = localStorage.getItem(key) || '';",
    "        if (v.length !== 7 || v.charAt(0) !== '#') {",
    "            return def;",
    "        }",
    "        const n: number = parseInt(v.substring(1), 16);",
    "        return isNaN(n) ? def : n;",
    "    }",
    "",
    "    /// custom: multiply a colour's channels (f > 1 lightens, f < 1 darkens), clamped —",
    "    /// how a 2004 palette gets a shade without a gradient",
    "    private static orbShade(rgb: number, f: number): number {",
    "        const r: number = Math.min(255, ((((rgb >> 16) & 0xff) * f) | 0));",
    "        const g: number = Math.min(255, ((((rgb >> 8) & 0xff) * f) | 0));",
    "        const b: number = Math.min(255, (((rgb & 0xff) * f) | 0));",
    "        return (r << 16) | (g << 8) | b;",
    "    }",
    "",
    "    // todo: order",
    "    private gameDraw(): void {"
   ],
   "note": "src/client/Client.ts @ old line 3888",
   "occ": 1,
   "pos": [
    3888,
    0,
    3886,
    3889
   ]
  },
  {
   "find": [
    "        this.areaMap?.setPixels();",
    "",
    "        if (this.minimapState == 2) {",
    "            if (this.mapback !== null) {"
   ],
   "replace": [
    "        this.areaMap?.setPixels();",
    "",
    "        // custom (lclite \"stat-orbs\"): read our OWN key here (runs every frame); lclite:stat-orbs",
    "        // one-time wipe of orb pixels left in the widget buffer after the",
    "        // toggle goes off mid-session (the stone strip is not redrawn per tick)",
    "        this.orbsEnabled = localStorage.getItem('statOrbs') === 'true';",
    "        if (this.orbsWereOn && !this.orbsEnabled) {",
    "            this.orbsWereOn = false;",
    "            this.mapback?.plotSprite(0, 0);",
    "        }",
    "",
    "        if (this.minimapState == 2) {",
    "            if (this.mapback !== null) {"
   ],
   "note": "src/client/Client.ts @ old line 11334",
   "occ": 1,
   "pos": [
    11334,
    0,
    11332,
    11335
   ]
  },
  {
   "find": [
    "            this.compass?.scanlineRotatePlotSprite(0, 0, 33, 33, 25, 25, this.orbitCameraYaw, 256, this.compassMaskLineOffsets, this.compassMaskLineLengths);",
    "",
    "            this.areaGame?.setPixels();",
    "            return;"
   ],
   "replace": [
    "            this.compass?.scanlineRotatePlotSprite(0, 0, 33, 33, 25, 25, this.orbitCameraYaw, 256, this.compassMaskLineOffsets, this.compassMaskLineLengths);",
    "",
    "            // custom: orbs survive the hidden-minimap state too lclite:stat-orbs",
    "            if (this.orbsEnabled) {",
    "                this.drawStatOrbs();",
    "                this.orbsWereOn = true;",
    "            }",
    "",
    "            this.areaGame?.setPixels();",
    "            return;"
   ],
   "note": "src/client/Client.ts @ old line 11348",
   "occ": 1,
   "pos": [
    11348,
    0,
    11346,
    11349
   ]
  },
  {
   "find": [
    "        Pix2D.fillRect(97, 78, 3, 3, Colour.WHITE);",
    "",
    "        this.areaGame?.setPixels();",
    "    }"
   ],
   "replace": [
    "        Pix2D.fillRect(97, 78, 3, 3, Colour.WHITE);",
    "",
    "        // custom: OSRS-style stat orbs (lclite TYPE B hook — see lclite/docs/MODS.md) lclite:stat-orbs",
    "        // drawn into the minimap widget buffer so they ride ON TOP of the interface",
    "        if (this.orbsEnabled) {",
    "            this.drawStatOrbs();",
    "            this.orbsWereOn = true;",
    "        }",
    "",
    "        this.areaGame?.setPixels();",
    "    }"
   ],
   "note": "src/client/Client.ts @ old line 11449",
   "occ": 1,
   "pos": [
    11449,
    0,
    11447,
    11450
   ]
  }
 ]
}